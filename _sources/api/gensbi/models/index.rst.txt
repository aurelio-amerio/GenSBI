gensbi.models
=============

.. py:module:: gensbi.models

.. autoapi-nested-parse::

   
   Model architectures for GenSBI.

   This package provides transformer-based models for simulation-based inference,
   including Flux1, Simformer, and autoencoder architectures, along with their
   associated loss functions and wrappers.















   ..
       !! processed by numpydoc !!


Submodules
----------

.. toctree::
   :maxdepth: 1

   /api/gensbi/models/core/index
   /api/gensbi/models/embedding/index
   /api/gensbi/models/flux1/index
   /api/gensbi/models/flux1joint/index
   /api/gensbi/models/maf/index
   /api/gensbi/models/simformer/index
   /api/gensbi/models/tarflow/index
   /api/gensbi/models/wrappers/index


Classes
-------

.. autoapisummary::

   gensbi.models.ConditionalWrapper
   gensbi.models.Flux1
   gensbi.models.Flux1Joint
   gensbi.models.Flux1JointParams
   gensbi.models.Flux1Params
   gensbi.models.JointWrapper
   gensbi.models.MAFlow
   gensbi.models.MAFlowParams
   gensbi.models.Simformer
   gensbi.models.SimformerParams
   gensbi.models.TarFlow
   gensbi.models.TarFlowParams
   gensbi.models.UnconditionalWrapper


Package Contents
----------------

.. py:class:: ConditionalWrapper(model)

   Bases: :py:obj:`gensbi.utils.model_wrapping.ModelWrapper`


   
   Wrapper for conditional models to handle input expansion and calling convention.

   :param model:
   :type model: The conditional model instance to wrap.















   ..
       !! processed by numpydoc !!

   .. py:method:: __call__(t, obs, obs_ids, cond, cond_ids, conditioned = True, guidance = None, **kwargs)

      
      Call the wrapped model with expanded inputs.

      :param t: Time steps.
      :type t: Array
      :param obs: Observations.
      :type obs: Array
      :param obs_ids: Observation identifiers.
      :type obs_ids: Array
      :param cond: Conditioning values.
      :type cond: Array
      :param cond_ids: Conditioning identifiers.
      :type cond_ids: Array
      :param conditioned: Whether to use conditioning. Defaults to True.
      :type conditioned: bool | Array, optional
      :param guidance: Optional guidance input.
      :type guidance: Array | None, optional

      :returns: Model output.
      :rtype: Array















      ..
          !! processed by numpydoc !!


.. py:class:: Flux1(params)

   Bases: :py:obj:`flax.nnx.Module`


   
   Transformer model for flow matching on sequences.
















   ..
       !! processed by numpydoc !!

   .. py:method:: __call__(t, obs, obs_ids, cond, cond_ids, conditioned = True, guidance = None)


   .. py:attribute:: cond_in


   .. py:attribute:: double_blocks


   .. py:attribute:: final_layer


   .. py:attribute:: hidden_size


   .. py:attribute:: id_merge_mode


   .. py:attribute:: in_channels


   .. py:attribute:: num_heads


   .. py:attribute:: obs_in


   .. py:attribute:: out_channels


   .. py:attribute:: params


   .. py:attribute:: qkv_features


   .. py:attribute:: single_blocks


   .. py:attribute:: time_in


   .. py:attribute:: vector_in


.. py:class:: Flux1Joint(params)

   Bases: :py:obj:`flax.nnx.Module`


   
   Flux1Joint model for joint density estimation.

   :param params: Parameters for the Flux1Joint model.
   :type params: Flux1JointParams















   ..
       !! processed by numpydoc !!

   .. py:method:: __call__(t, obs, node_ids, condition_mask, guidance = None, edge_mask = None)


   .. py:attribute:: final_layer


   .. py:attribute:: hidden_size


   .. py:attribute:: in_channels


   .. py:attribute:: num_heads


   .. py:attribute:: obs_in


   .. py:attribute:: out_channels


   .. py:attribute:: params


   .. py:attribute:: qkv_features


   .. py:attribute:: single_blocks


   .. py:attribute:: time_in


   .. py:attribute:: vector_in


.. py:class:: Flux1JointParams

   
   Parameters for the Flux1Joint model.

   GenSBI uses the tensor convention `(batch, dim, channels)`.

   For joint density estimation, the model consumes a *single* sequence `obs` that
   mixes all variables you want to model jointly. In this case:

   - `dim_joint` is the number of tokens in that joint sequence.
   - `in_channels` is the number of channels/features per token.

   In many SBI-style problems you will still use `in_channels = 1` (one scalar per token),
   but for some datasets a token may carry multiple features.

   :param in_channels: Number of channels/features per token.
   :type in_channels: int
   :param vec_in_dim: Dimension of the vector input, if applicable.
   :type vec_in_dim: Union[int, None]
   :param mlp_ratio: Ratio for the MLP layers.
   :type mlp_ratio: float
   :param num_heads: Number of attention heads.
   :type num_heads: int
   :param depth_single_blocks: Number of single stream blocks.
   :type depth_single_blocks: int
   :param val_emb_dim: Number of features per head used to embed the data.
   :type val_emb_dim: int
   :param cond_emb_dim: Number of features per head used to encode the condition mask, which determines the features on which we are conditioning.
   :type cond_emb_dim: int
   :param id_emb_dim: Number of features per head used to encode the token ids.
   :type id_emb_dim: int
   :param qkv_bias: Whether to use bias in QKV layers.
   :type qkv_bias: bool
   :param rngs: Random number generators for initialization.
   :type rngs: nnx.Rngs
   :param dim_joint: Number of tokens in the joint sequence.
   :type dim_joint: int
   :param id_merge_mode: Strategy for combining embeddings ("sum" or "concat").
   :type id_merge_mode: str
   :param id_embedding_strategy: Kind of ID embedding. Currently only "absolute" is supported for Flux1Joint.
   :type id_embedding_strategy: str
   :param guidance_embed: Whether to use guidance embedding.
   :type guidance_embed: bool
   :param param_dtype: Data type for model parameters.
   :type param_dtype: DTypeLike















   ..
       !! processed by numpydoc !!

   .. py:method:: __post_init__()


   .. py:attribute:: cond_emb_dim
      :type:  int


   .. py:attribute:: depth_single_blocks
      :type:  int


   .. py:attribute:: dim_joint
      :type:  int


   .. py:attribute:: guidance_embed
      :type:  bool
      :value: False



   .. py:attribute:: id_emb_dim
      :type:  int


   .. py:attribute:: id_embedding_strategy
      :type:  str
      :value: 'absolute'



   .. py:attribute:: id_merge_mode
      :type:  str
      :value: 'sum'



   .. py:attribute:: in_channels
      :type:  int


   .. py:attribute:: mlp_ratio
      :type:  float


   .. py:attribute:: num_heads
      :type:  int


   .. py:attribute:: param_dtype
      :type:  jax.typing.DTypeLike


   .. py:attribute:: qkv_bias
      :type:  bool


   .. py:attribute:: rngs
      :type:  flax.nnx.Rngs


   .. py:attribute:: val_emb_dim
      :type:  int


   .. py:attribute:: vec_in_dim
      :type:  Union[int, None]


.. py:class:: Flux1Params

   
   Parameters for the Flux1 model.

   GenSBI uses the tensor convention `(batch, dim, channels)`.

   - `dim_*` counts **tokens** (how many distinct observables/variables you have).
   - `channels` counts **features per token** (how many values each observable carries).

   For conditional SBI with Flux1:

   - Parameters to infer (often denoted $      heta$) have shape `(batch, dim_obs, in_channels)`.
       In most SBI problems `in_channels = 1` (one scalar per parameter token).
   - Conditioning data (often denoted $x$) has shape `(batch, dim_cond, context_in_dim)`.
       `context_in_dim` can be > 1 (e.g., multiple detectors or multiple features per measured token).

   **Data Stucture and ID Embeddings**:

   Flux1 supports unstructured, 1D, and 2D data (and can be extended to ND) through different ID embedding strategies.
   The model needs to know *what* each token represents distinct from its value. This is handled by `id_embedding_strategy`.

   - `absolute`: Learned embeddings. Use for **unstructured data** (order doesn't matter, e.g. physical parameters).
       Initialize IDs using `gensbi.recipes.utils.init_ids_1d` (the `semantic_id` will be ignored).
   - `pos1d` / `rope1d`: 1D positional embeddings. Use for **sequential data** (order matters, e.g. time series, spectra).
       Initialize IDs using `gensbi.recipes.utils.init_ids_1d`. The `semantic_id` is optional for `pos1d` but recommended for `rope1d`.
   - `pos2d` / `rope2d`: 2D positional embeddings. Use for **image data** or 2D grids.
       Initialize IDs using `gensbi.recipes.utils.init_ids_2d`. The `semantic_id` is optional for `pos2d` but recommended for `rope2d`.

   **Combining ID Embeddings**:

   Strategies for combining the value and ID embeddings (`id_merge_mode`):

   - `"sum"` (Default): The value and ID embeddings are summed. This is the standard approach for large transformers.
     Requires `axes_dim` to be specified.
     **Recommended for**: Large models, high-dimensional data, or when using RoPE.
   - `"concat"`: The value and ID embeddings are concatenated.
     Requires `val_emb_dim` (features for value) and `id_emb_dim` (features for ID) to be specified.
     **Recommended for**: Small models (low dimension per head, few heads) to reduce confusion between value and positional information.
     A good starting ratio for `val_emb_dim : id_emb_dim` is **1:1**.

   **Preprocessing for Images/2D Data**:

   - **Patchification**: 2D images must be patchified (flattened into a sequence of tokens) before passing them to the model.
     Use `gensbi.models.core.patching.patchify_2d` for this purpose.
   - **Normalization**: To speed up convergence, ensure data is normalized to 0 mean and unit variance.

   .. note::
       See the documentation and tutorials for more information on id embeddings and data preprocessing.

   :param in_channels: Number of channels per observation/parameter token.
   :type in_channels: int
   :param vec_in_dim: Dimension of the vector input, if applicable.
   :type vec_in_dim: Union[int, None]
   :param context_in_dim: Number of channels per conditioning token.
   :type context_in_dim: int
   :param mlp_ratio: Ratio for the MLP layers.
   :type mlp_ratio: float
   :param num_heads: Number of attention heads.
   :type num_heads: int
   :param depth: Number of double stream blocks.
   :type depth: int
   :param depth_single_blocks: Number of single stream blocks.
   :type depth_single_blocks: int
   :param qkv_bias: Whether to use bias in QKV layers.
   :type qkv_bias: bool
   :param rngs: Random number generators for initialization.
   :type rngs: nnx.Rngs
   :param dim_obs: Number of observation/parameter tokens.
   :type dim_obs: int
   :param dim_cond: Number of conditioning tokens.
   :type dim_cond: int
   :param axes_dim: Dimensions of the axes for positional encoding (required for "sum" strategy).
   :type axes_dim: Optional[list[int]]
   :param val_emb_dim: Features per head for value embedding (required for "concat" strategy).
   :type val_emb_dim: Optional[int]
   :param id_emb_dim: Features per head for ID embedding (required for "concat" strategy).
   :type id_emb_dim: Optional[int]
   :param id_merge_mode: Strategy for combining embeddings ("sum" or "concat"). Default is "sum".
   :type id_merge_mode: str
   :param theta: Scaling factor for positional encoding.
   :type theta: Optional[int]
   :param id_embedding_strategy: Kind of ID embedding for obs and cond respectively. Options are "absolute", "pos1d", "pos2d", "rope1d", "rope2d".
   :type id_embedding_strategy: tuple[str, str]
   :param guidance_embed: Whether to use guidance embedding.
   :type guidance_embed: bool
   :param param_dtype: Data type for model parameters.
   :type param_dtype: DTypeLike















   ..
       !! processed by numpydoc !!

   .. py:method:: __post_init__()


   .. py:attribute:: axes_dim
      :type:  Optional[list[int]]
      :value: None



   .. py:attribute:: context_in_dim
      :type:  int


   .. py:attribute:: depth
      :type:  int


   .. py:attribute:: depth_single_blocks
      :type:  int


   .. py:attribute:: dim_cond
      :type:  int


   .. py:attribute:: dim_obs
      :type:  int


   .. py:attribute:: guidance_embed
      :type:  bool
      :value: False



   .. py:attribute:: id_emb_dim
      :type:  Optional[int]
      :value: None



   .. py:attribute:: id_embedding_strategy
      :type:  tuple[str, str]
      :value: ('absolute', 'absolute')



   .. py:attribute:: id_merge_mode
      :type:  str
      :value: 'sum'



   .. py:attribute:: in_channels
      :type:  int


   .. py:attribute:: mlp_ratio
      :type:  float


   .. py:attribute:: num_heads
      :type:  int


   .. py:attribute:: param_dtype
      :type:  jax.typing.DTypeLike


   .. py:attribute:: qkv_bias
      :type:  bool


   .. py:attribute:: rngs
      :type:  flax.nnx.Rngs


   .. py:attribute:: theta
      :type:  Optional[int]
      :value: None



   .. py:attribute:: val_emb_dim
      :type:  Optional[int]
      :value: None



   .. py:attribute:: vec_in_dim
      :type:  Union[int, None]


.. py:class:: JointWrapper(model)

   Bases: :py:obj:`gensbi.utils.model_wrapping.ModelWrapper`


   
   Wrapper for joint models to handle both conditioned and unconditioned inference.

   :param model:
   :type model: The joint model instance to wrap.
   :param conditioned: Whether to use conditioning by default. Defaults to True.
   :type conditioned: bool, optional















   ..
       !! processed by numpydoc !!

   .. py:method:: __call__(t, obs, obs_ids, cond, cond_ids, conditioned = True, **kwargs)

      
      Call the wrapped model for either conditioned or unconditioned inference.

      :param t: Time steps.
      :type t: Array
      :param obs: Observations.
      :type obs: Array
      :param obs_ids: Observation identifiers.
      :type obs_ids: Array
      :param cond: Conditioning values.
      :type cond: Array
      :param cond_ids: Conditioning identifiers.
      :type cond_ids: Array
      :param conditioned: Whether to use conditioning. If None, uses the default set at initialization.
      :type conditioned: bool, optional
      :param \*\*kwargs:
      :type \*\*kwargs: Additional keyword arguments passed to the model.

      :returns: Model output.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:method:: conditioned(obs, obs_ids, cond, cond_ids, t, **kwargs)

      
      Perform conditioned inference.

      :param obs: Observations.
      :type obs: Array
      :param obs_ids: Observation identifiers.
      :type obs_ids: Array
      :param cond: Conditioning values.
      :type cond: Array
      :param cond_ids: Conditioning identifiers.
      :type cond_ids: Array
      :param t: Time steps.
      :type t: Array
      :param \*\*kwargs:
      :type \*\*kwargs: Additional keyword arguments passed to the model.

      :returns: Conditioned output (only for unconditioned variables).
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:method:: unconditioned(obs, obs_ids, t, **kwargs)

      
      Perform unconditioned inference.

      :param obs: Observations.
      :type obs: Array
      :param obs_ids: Observation identifiers.
      :type obs_ids: Array
      :param t: Time steps.
      :type t: Array
      :param \*\*kwargs:
      :type \*\*kwargs: Additional keyword arguments passed to the model.

      :returns: Unconditioned output.
      :rtype: Array















      ..
          !! processed by numpydoc !!


.. py:class:: MAFlow(params)

   Bases: :py:obj:`flax.nnx.Module`


   
   Masked Autoregressive Flow for exact density evaluation and sampling.

   Stacks :class:`~gensbi.models.maf.made.MaskedAutoregressive` layers
   separated by permutations, with an optional data-end
   :class:`~gensbi.normalizing_flows.bijections.standardize.Standardize`
   bijection, over a standard-normal base distribution.

   Log-density follows the change-of-variables formula:
   ``log_prob(x, cond) = base.log_prob(u) + logdet``, where
   ``u, logdet = chain.inverse(x, cond)``.  The base distribution is built
   lazily and never enters ``nnx`` state.

   :param params: Full architecture configuration; see :class:`MAFlowParams`.
   :type params: MAFlowParams















   ..
       !! processed by numpydoc !!

   .. py:method:: _base()


   .. py:method:: log_prob(x, cond = None)

      
      Compute the change-of-variables log-density for a batch of samples.

      :param x: Data batch.  Shape ``(B, dim)`` when ``channels == 1``, or
                ``(B, dim, C)`` when ``channels > 1`` (the channel axis is
                flattened internally to ``(B, dim * C)``).
      :type x: Array
      :param cond: Conditioning batch of shape ``(B, cond_dim)`` for
                   ``cond_channels == 1``, or ``(B, cond_dim, C_cond)`` for
                   ``cond_channels > 1`` (also flattened internally).  Pass
                   ``None`` for an unconditional model.
      :type cond: Array or None, optional

      :returns: Log-probability of shape ``(B,)``.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:method:: sample(key, cond = None, nsamples = None)

      
      Draw samples from the flow.

      :param key: Random key.
      :type key: jax.random.PRNGKey
      :param cond: Conditioning batch of shape ``(nsamples, cond_dim)`` for
                   ``cond_channels == 1``, or ``(nsamples, cond_dim, C_cond)`` for
                   ``cond_channels > 1`` (flattened internally).  If provided, the
                   number of samples is inferred from ``cond.shape[0]`` and
                   ``nsamples`` is ignored.
      :type cond: Array or None, optional
      :param nsamples: Number of samples to draw.  Required when ``cond`` is ``None``.
      :type nsamples: int or None, optional

      :returns: Sample array of shape ``(nsamples, dim, channels)`` for all ``C >= 1``
                (``C = 1`` gives ``(nsamples, dim, 1)``; channel axis is never collapsed).
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:method:: set_standardization(mean, std)

      
      Set the data-end Standardize bijection's mean/std buffers in place.

      Accepts shapes ``(dim,)`` (broadcast to ``(dim, 1)``), ``(dim, 1)``,
      ``(C,)`` (per-channel broadcast), or a scalar broadcastable to
      ``(dim, channels)``.

      Raises ValueError if built with ``standardize=False``.















      ..
          !! processed by numpydoc !!


   .. py:attribute:: chain


   .. py:attribute:: channels


   .. py:attribute:: cond_channels


   .. py:attribute:: cond_dim


   .. py:attribute:: dim


   .. py:attribute:: flat_dim


.. py:class:: MAFlowParams

   
   Architecture parameters for :class:`MAFlow`.

   Only ``rngs`` and ``dim`` are required. ``transformer`` defaults to
   ``Affine()`` (pass ``RQSpline()`` for a spline flow).

   :param rngs: Flax RNG container used to initialise all trainable parameters.
   :type rngs: nnx.Rngs
   :param dim: Dimensionality of the target variable.
   :type dim: int
   :param cond_dim: Dimensionality of the conditioning input.  Default is 0 (unconditional).
   :type cond_dim: int, optional
   :param n_layers: Number of :class:`~gensbi.models.maf.made.MaskedAutoregressive` layers.
                    Default is 5.
   :type n_layers: int, optional
   :param transformer: Elementwise bijection used by each autoregressive layer.  If ``None``
                       (default), an
                       :class:`~gensbi.normalizing_flows.bijections.transformers.Affine`
                       bijection is constructed automatically in ``__post_init__``.
   :type transformer: Bijection or None, optional
   :param nn_width: Width of each hidden layer in the MADE conditioner network.
                    Default is 64.
   :type nn_width: int, optional
   :param nn_depth: Number of hidden layers in the MADE conditioner network.  Default is 2.
   :type nn_depth: int, optional
   :param permutation: Permutation applied between autoregressive layers.  ``"reverse"``
                       (default) reverses the dimension ordering; ``"random"`` applies a
                       random shuffle sampled at construction time.
   :type permutation: str, optional
   :param standardize: If ``True`` (default), append a
                       :class:`~gensbi.normalizing_flows.bijections.standardize.Standardize`
                       bijection at the data end of the chain.
   :type standardize: bool, optional
   :param zero_init: If ``True`` (default), zero-initialise the output layer of each MADE
                     network so the flow starts as an identity transform.
   :type zero_init: bool, optional















   ..
       !! processed by numpydoc !!

   .. py:method:: __post_init__()


   .. py:attribute:: channels
      :type:  int
      :value: 1



   .. py:attribute:: cond_channels
      :type:  int
      :value: 1



   .. py:attribute:: cond_dim
      :type:  int
      :value: 0



   .. py:attribute:: dim
      :type:  int


   .. py:attribute:: n_layers
      :type:  int
      :value: 5



   .. py:attribute:: nn_depth
      :type:  int
      :value: 2



   .. py:attribute:: nn_width
      :type:  int
      :value: 64



   .. py:attribute:: permutation
      :type:  str
      :value: 'reverse'



   .. py:attribute:: rngs
      :type:  flax.nnx.Rngs


   .. py:attribute:: standardize
      :type:  bool
      :value: True



   .. py:attribute:: transformer
      :type:  gensbi.normalizing_flows.bijections.base.Bijection | None
      :value: None



   .. py:attribute:: zero_init
      :type:  bool
      :value: True



.. py:class:: Simformer(params, embedding_net_value = None)

   Bases: :py:obj:`flax.nnx.Module`


   
   Simformer model for joint density estimation.

   :param params: Parameters for the Simformer model.
   :type params: SimformerParams















   ..
       !! processed by numpydoc !!

   .. py:method:: __call__(t, obs, node_ids, condition_mask, edge_mask = None)

      
      Forward pass of the Simformer model.

      :param t: Time steps.
      :type t: Array
      :param obs: Input data.
      :type obs: Array
      :param args: Additional arguments.
      :type args: Optional[dict]
      :param node_ids: Node identifiers.
      :type node_ids: Array
      :param condition_mask: Mask for conditioning.
      :type condition_mask: Array
      :param edge_mask: Mask for edges.
      :type edge_mask: Optional[Array]

      :returns: Model output.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:attribute:: cond_emb_dim


   .. py:attribute:: condition_embedding


   .. py:attribute:: embedding_net_id


   .. py:attribute:: embedding_time


   .. py:attribute:: id_emb_dim


   .. py:attribute:: in_channels


   .. py:attribute:: output_fn


   .. py:attribute:: params


   .. py:attribute:: total_tokens


   .. py:attribute:: transformer


   .. py:attribute:: val_emb_dim


.. py:class:: SimformerParams

   
   Parameters for the Simformer model.

   GenSBI uses the tensor convention `(batch, dim, channels)`.

   For Simformer (joint modeling), the input `obs` is a single sequence with:

   - `dim_joint`: number of tokens in the sequence (how many variables / measured points).
   - `in_channels`: number of channels/features per token.

   Conditioning is controlled via `condition_mask` at call time (mask is over **tokens**,
   not channels): tokens with mask=1 are treated as conditioned.

   :param rngs: Random number generators for initialization.
   :type rngs: nnx.Rngs
   :param in_channels: Number of channels/features per token.
   :type in_channels: int
   :param val_emb_dim: Dimension of the value embeddings.
   :type val_emb_dim: int
   :param id_emb_dim: Dimension of the ID embeddings.
   :type id_emb_dim: int
   :param cond_emb_dim: Dimension of the condition embeddings.
   :type cond_emb_dim: int
   :param dim_joint: Number of tokens in the joint sequence.
   :type dim_joint: int
   :param fourier_features: Number of Fourier features for time embedding.
   :type fourier_features: int
   :param num_heads: Number of attention heads.
   :type num_heads: int
   :param depth: Number of transformer layers.
   :type depth: int
   :param mlp_ratio: Widening factor for the transformer parameters (MLP ratio).
   :type mlp_ratio: int
   :param qkv_features: Number of features for QKV layers.
   :type qkv_features: int
   :param num_hidden_layers: Number of hidden layers in the transformer.
   :type num_hidden_layers: int
   :param param_dtype: Data type for model parameters.
   :type param_dtype: DTypeLike















   ..
       !! processed by numpydoc !!

   .. py:method:: __post_init__()


   .. py:attribute:: cond_emb_dim
      :type:  int


   .. py:attribute:: depth
      :type:  int


   .. py:attribute:: dim_joint
      :type:  int


   .. py:attribute:: fourier_features
      :type:  int
      :value: 128



   .. py:attribute:: id_emb_dim
      :type:  int


   .. py:attribute:: in_channels
      :type:  int


   .. py:attribute:: mlp_ratio
      :type:  int
      :value: 3



   .. py:attribute:: num_heads
      :type:  int


   .. py:attribute:: num_hidden_layers
      :type:  int
      :value: 1



   .. py:attribute:: param_dtype
      :type:  jax.typing.DTypeLike


   .. py:attribute:: qkv_features
      :type:  int | None
      :value: None



   .. py:attribute:: rngs
      :type:  flax.nnx.Rngs


   .. py:attribute:: val_emb_dim
      :type:  int


.. py:class:: TarFlow(params)

   Bases: :py:obj:`flax.nnx.Module`


   
   Transformer autoregressive normalizing flow density model.

   Stacks :class:`~gensbi.models.tarflow.blocks.MetaBlock` bijections with
   alternating token permutations on top of a tokenizer and an isotropic
   Gaussian base distribution. Supports both vector and image data, with
   optional input standardization.

   :param params: Architecture and initialization parameters.
   :type params: TarFlowParams















   ..
       !! processed by numpydoc !!

   .. py:method:: _base_log_prob(z)


   .. py:method:: _ensure_batched(x)


   .. py:method:: log_prob(x, cond = None)

      
      Compute the log-probability of data under the model.

      Applies standardization, tokenizes the input, then runs each
      :class:`~gensbi.models.tarflow.blocks.MetaBlock`'s
      :meth:`~gensbi.models.tarflow.blocks.MetaBlock.inverse` transform
      (data→noise direction), accumulating the log-absolute-determinant
      terms, and finally evaluates the base Gaussian log-probability.

      :param x: Data samples of shape ``(B, *example_shape)`` or a single
                unbatched sample that will be promoted to a batch of one.
      :type x: Array
      :param cond: Conditioning batch of shape ``(B, cond_dim)`` for
                   ``cond_channels == 1``, or ``(B, cond_dim, C_cond)`` for
                   ``cond_channels > 1`` (flattened internally by the conditioner).
                   Pass ``None`` for an unconditional model.
      :type cond: Array or None, optional

      :returns: Log-probabilities of shape ``(B,)``.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:method:: sample(key, cond = None, nsamples = None)

      
      Draw samples from the model.

      Samples noise from ``N(0, I)``, then applies each
      :class:`~gensbi.models.tarflow.blocks.MetaBlock`'s
      :meth:`~gensbi.models.tarflow.blocks.MetaBlock.forward` transform
      (noise→data direction) in reverse block order, detokenizes the
      result, and applies the inverse standardization.

      :param key: Random key for noise sampling.
      :type key: jax.random.PRNGKey
      :param cond: Conditioning batch of shape ``(nsamples, cond_dim)`` for
                   ``cond_channels == 1``, or ``(nsamples, cond_dim, C_cond)`` for
                   ``cond_channels > 1`` (flattened internally by the conditioner).
                   If provided, ``nsamples`` is inferred from ``cond.shape[0]``.
      :type cond: Array or None, optional
      :param nsamples: Number of samples to draw. Required when ``cond`` is ``None``.
      :type nsamples: int or None, optional

      :returns: Samples of shape ``(B, *example_shape)``.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:method:: set_standardization(mean, std)

      
      Set the mean and standard deviation for input standardization.

      Accepts shapes ``(dim,)`` (broadcast to ``(dim, 1)``), ``(dim, 1)``,
      ``(C,)`` (per-channel broadcast), or a scalar broadcastable to
      ``example_shape``.

      :param mean: Mean broadcastable to ``example_shape``.
      :type mean: Array
      :param std: Standard deviation broadcastable to ``example_shape``.
      :type std: Array

      :rtype: None

      :raises ValueError: If the model was built with ``standardize=False``.















      ..
          !! processed by numpydoc !!


   .. py:attribute:: F
      :value: 1



   .. py:attribute:: T


   .. py:attribute:: _standardize


   .. py:attribute:: blocks


   .. py:attribute:: cond_dim


   .. py:attribute:: dim


   .. py:attribute:: example_shape


   .. py:attribute:: mean


   .. py:attribute:: std


   .. py:attribute:: tokenizer


.. py:class:: TarFlowParams

   
   Architecture parameters for :class:`TarFlow`.

   ``modeled`` selects the tokenizer (``"vector"`` or ``"image"``); ``cond``
   selects the conditioner (``"bias"``, ``"vector"``, or
   ``"image"``). Head sizing follows the Flux1 convention: specify
   ``head_dim`` and ``num_heads``; total width
   ``channels = head_dim * num_heads`` is derived in ``__post_init__``.

   :param rngs: Flax RNG container passed to all sub-modules during construction.
   :type rngs: nnx.Rngs
   :param dim: Feature dimension of each input vector. Required when
               ``modeled="vector"``. Default is ``None``.
   :type dim: int or None, optional
   :param cond_dim: Dimensionality of the conditioning vector. Set to ``0`` for an
                    unconditional model. Default is ``0``.
   :type cond_dim: int, optional
   :param modeled: Tokenizer type: ``"vector"`` (1-D data) or ``"image"`` (spatial data).
                   Default is ``"vector"``.
   :type modeled: str, optional
   :param img_size: Spatial size (height = width) of the modeled image. Required when
                    ``modeled="image"``. Default is ``None``.
   :type img_size: int or None, optional
   :param patch_size: Patch size for the image tokenizer. Required when
                      ``modeled="image"``. Default is ``None``.
   :type patch_size: int or None, optional
   :param img_channels: Number of channels in the modeled image. Default is ``1``.
   :type img_channels: int, optional
   :param cond: Conditioning strategy: ``"bias"`` (per-token additive bias via
                :class:`~gensbi.models.tarflow.conditioners.AdditiveBiasConditioner`),
                ``"vector"`` (one condition token per modeled coordinate via
                :class:`~gensbi.models.tarflow.conditioners.VectorConditioner`),
                or ``"image"`` (prefix tokens from an image via
                :class:`~gensbi.models.tarflow.conditioners.ImageConditioner`).
                Default is ``"bias"``.
   :type cond: str, optional
   :param cond_img_size: Spatial size of the conditioning image. Required when
                         ``cond="image"``. Default is ``None``.
   :type cond_img_size: int or None, optional
   :param cond_patch_size: Patch size for the image conditioning tokenizer. Required when
                           ``cond="image"``. Default is ``None``.
   :type cond_patch_size: int or None, optional
   :param cond_channels: Number of channels in the conditioning image. Default is ``1``.
   :type cond_channels: int, optional
   :param head_dim: Dimension per attention head. Default is ``16``.
   :type head_dim: int, optional
   :param num_heads: Number of attention heads per block. Default is ``4``.
   :type num_heads: int, optional
   :param num_blocks: Number of :class:`~gensbi.models.tarflow.blocks.MetaBlock` layers.
                      Default is ``8``.
   :type num_blocks: int, optional
   :param layers_per_block: Number of :class:`~gensbi.models.tarflow.blocks.AttentionBlock`
                            layers inside each :class:`~gensbi.models.tarflow.blocks.MetaBlock`.
                            Default is ``2``.
   :type layers_per_block: int, optional
   :param block_size: Token grouping factor for the vector tokenizer. Default is ``1``.
   :type block_size: int, optional
   :param permutation: Token permutation strategy per block: ``"flip"`` (alternate
                       forward/reverse order) or ``"random"`` (independently sampled per
                       block). Default is ``"flip"``.
   :type permutation: str, optional
   :param standardize: If ``True`` (default), apply mean/std standardization to inputs and
                       outputs. Enables :meth:`TarFlow.set_standardization`.
   :type standardize: bool, optional
   :param zero_init: If ``True`` (default), initialize ``proj_out`` weights to zero so
                     each :class:`~gensbi.models.tarflow.blocks.MetaBlock` starts as the
                     identity map.
   :type zero_init: bool, optional
   :param use_softplus: If ``True`` (default), use softplus for the affine scale (numerically
                        stable, bounded tail). If ``False``, use ``exp`` (legacy behavior).
   :type use_softplus: bool, optional
   :param soft_clip: Soft-clip magnitude applied via ``tanh`` to raw network outputs before
                     splitting into ``(a, b)``. Default is ``4.0``.
   :type soft_clip: float, optional















   ..
       !! processed by numpydoc !!

   .. py:method:: __post_init__()


   .. py:attribute:: block_size
      :type:  int
      :value: 1



   .. py:attribute:: cond
      :type:  str
      :value: 'bias'



   .. py:attribute:: cond_channels
      :type:  int
      :value: 1



   .. py:attribute:: cond_dim
      :type:  int
      :value: 0



   .. py:attribute:: cond_img_size
      :type:  int | None
      :value: None



   .. py:attribute:: cond_patch_size
      :type:  int | None
      :value: None



   .. py:attribute:: dim
      :type:  int | None
      :value: None



   .. py:attribute:: head_dim
      :type:  int
      :value: 16



   .. py:attribute:: img_channels
      :type:  int
      :value: 1



   .. py:attribute:: img_size
      :type:  int | None
      :value: None



   .. py:attribute:: layers_per_block
      :type:  int
      :value: 2



   .. py:attribute:: modeled
      :type:  str
      :value: 'vector'



   .. py:attribute:: num_blocks
      :type:  int
      :value: 8



   .. py:attribute:: num_heads
      :type:  int
      :value: 4



   .. py:attribute:: patch_size
      :type:  int | None
      :value: None



   .. py:attribute:: permutation
      :type:  str
      :value: 'flip'



   .. py:attribute:: rngs
      :type:  flax.nnx.Rngs


   .. py:attribute:: soft_clip
      :type:  float
      :value: 4.0



   .. py:attribute:: standardize
      :type:  bool
      :value: True



   .. py:attribute:: use_softplus
      :type:  bool
      :value: True



   .. py:attribute:: vec_channels
      :type:  int
      :value: 1



   .. py:attribute:: zero_init
      :type:  bool
      :value: True



.. py:class:: UnconditionalWrapper(model)

   Bases: :py:obj:`gensbi.utils.model_wrapping.ModelWrapper`


   
   Wrapper for unconditional models to handle input expansion and calling convention.

   :param model:
   :type model: The unconditional model instance to wrap.















   ..
       !! processed by numpydoc !!

   .. py:method:: __call__(t, obs, obs_ids, **kwargs)

      
      Call the wrapped model with expanded inputs.

      :param t: Time steps.
      :type t: Array
      :param obs: Observations.
      :type obs: Array
      :param obs_ids: Observation identifiers.
      :type obs_ids: Array
      :param \*\*kwargs:
      :type \*\*kwargs: Additional keyword arguments passed to the model.

      :returns: Model output.
      :rtype: Array















      ..
          !! processed by numpydoc !!


