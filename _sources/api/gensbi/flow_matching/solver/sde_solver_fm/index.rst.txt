gensbi.flow_matching.solver.sde_solver_fm
=========================================

.. py:module:: gensbi.flow_matching.solver.sde_solver_fm

.. autoapi-nested-parse::

   Flow Matching SDE Solvers.

   This module provides stochastic sampling for flow matching models,
   using SDE formulations from arXiv:2410.02217.

   **Time direction convention:**
   In flow matching, ``t=0`` is noise and ``t=1`` is data.
   These SDE solvers integrate **forward** from ``t=eps`` to ``t=1`` to transform
   noise into data, matching the ODE solver direction.
   This is the opposite of standard score matching (reverse SDE: ``t=T→eps``).

   **Input shape convention:**
   All inputs must have shape ``(batch, features, channels)``.
   If your data is 2D ``(batch, features)``, add a trailing dimension:
   ``x = x[..., None]``.

   Based on: "Improving Flow Matching by Stochastic Sampling"
   (arXiv:2410.02217)

   ..
       !! processed by numpydoc !!


Classes
-------

.. autoapisummary::

   gensbi.flow_matching.solver.sde_solver_fm.BaseFmSDESolver
   gensbi.flow_matching.solver.sde_solver_fm.NonSingularSolver
   gensbi.flow_matching.solver.sde_solver_fm.ZeroEndsSolver


Module Contents
---------------

.. py:class:: BaseFmSDESolver(velocity_model, mu0, sigma0, eps0 = 1e-05)

   Bases: :py:obj:`gensbi.solver.Solver`


   
   Base class for flow matching SDE solvers.

   Uses a velocity field model to construct drift and diffusion terms for an SDE
   that samples from the same distribution as the flow matching ODE, but with
   added stochasticity for improved sample quality.

   :param velocity_model: A velocity field model, properly wrapped.
   :type velocity_model: ModelWrapper
   :param mu0: Mean of the prior Gaussian distribution. Shape: ``(features, channels)``.
   :type mu0: Array
   :param sigma0: Standard deviation of the prior Gaussian distribution. Shape: ``(features, channels)``.
   :type sigma0: Array
   :param eps0: Minimum time value (to avoid singularities near t=0).
   :type eps0: float















   ..
       !! processed by numpydoc !!

   .. py:method:: get_f_tilde()
      :abstractmethod:


      
      Get the drift function :math:`\tilde{f}` for the SDE.

      See arXiv:2410.02217. Also known as the "drift" term.















      ..
          !! processed by numpydoc !!


   .. py:method:: get_g_tilde()
      :abstractmethod:


      
      Get the diffusion function :math:`\tilde{g}` for the SDE.

      See arXiv:2410.02217. Also known as the "diffusion" term.
      Must return a ``(flat_dim, flat_dim)`` matrix for ``ControlTerm``.















      ..
          !! processed by numpydoc !!


   .. py:method:: get_sampler(step_size, method = 'Euler', atol = 1e-05, rtol = 1e-05, time_grid = jnp.array([0.0, 1.0]), return_intermediates = False, static_model_kwargs = None)

      
      Stochastic sampler for the SDE.

      :param step_size: The step size. Must be None when using ``"ShARK"`` (adaptive step sizing).
      :type step_size: Optional[float]
      :param method: Integration method. One of ``"Euler"``, ``"Heun"``, ``"SEA"``,
                     ``"ShARK"``. Defaults to ``"Euler"``. ``"ShARK"`` automatically
                     uses adaptive step sizing via ``PIDController``; the others use
                     fixed step size.
      :type method: Union[str, diffrax.AbstractERK]
      :param atol: Absolute tolerance, used for adaptive step solvers.
      :type atol: float
      :param rtol: Relative tolerance, used for adaptive step solvers.
      :type rtol: float
      :param time_grid: The process is solved in the interval [min(time_grid), max(time_grid)] and if step_size is None then time discretization is set by the time grid. May specify a descending time_grid to solve in the reverse direction. Defaults to jnp.array([0.0, 1.0]).
      :type time_grid: Array
      :param return_intermediates: If True then return intermediate time steps according to time_grid. Defaults to False.
      :type return_intermediates: bool, optional
      :param static_model_kwargs: Static keyword arguments baked into the drift/diffusion
                                  at creation time.  Condition-dependent data should be
                                  passed at call time via ``model_extras``.
      :type static_model_kwargs: dict

      :returns: ``sampler(x_init, key, model_extras=None)`` that returns
                sampled trajectories of shape ``(batch, features, channels)``.
      :rtype: Callable















      ..
          !! processed by numpydoc !!


   .. py:method:: get_score(**kwargs)

      
      Obtain the score function given the velocity model. See arXiv:2410.02217.
















      ..
          !! processed by numpydoc !!


   .. py:method:: sample(x_init, step_size, method = 'Euler', atol = 1e-05, rtol = 1e-05, time_grid = jnp.array([0.0, 1.0]), return_intermediates = False, model_extras = None, key = None)

      
      Sample from the SDE.

      :param x_init: Initial conditions. Shape: [batch, features, channels].
      :type x_init: Array
      :param step_size: Step size.
      :type step_size: Optional[float]
      :param method: Integration method.
      :type method: Union[str, diffrax.AbstractERK]
      :param atol: Absolute tolerance.
      :type atol: float
      :param rtol: Relative tolerance.
      :type rtol: float
      :param time_grid: Time grid.
      :type time_grid: Array
      :param return_intermediates: Return intermediates.
      :type return_intermediates: bool
      :param model_extras: Runtime model extras (e.g. ``cond``, ``obs_ids``).
      :type model_extras: dict
      :param key: Random key. Required for SDE.
      :type key: jax.random.PRNGKey

      :returns: Samples.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:attribute:: eps0
      :value: 1e-05



   .. py:attribute:: flat_dim


   .. py:attribute:: mu0


   .. py:attribute:: prior_distribution


   .. py:attribute:: sample_shape


   .. py:attribute:: sigma0


   .. py:attribute:: velocity_model


.. py:class:: NonSingularSolver(velocity_model, mu0, sigma0, alpha)

   Bases: :py:obj:`BaseFmSDESolver`


   
   NonSingular SDE solver for flow matching.

   From Tab. 1 of `arXiv:2410.02217 <http://arxiv.org/abs/2410.02217>`_,
   with change of variable for time: t -> 1-t to match flow matching time notation.

   :param velocity_model: Velocity field model.
   :type velocity_model: ModelWrapper
   :param mu0: Prior mean, shape ``(features, channels)``.
   :type mu0: Array
   :param sigma0: Prior std, shape ``(features, channels)``.
   :type sigma0: Array
   :param alpha: Diffusion strength parameter.
   :type alpha: float















   ..
       !! processed by numpydoc !!

   .. py:method:: get_f_tilde(**kwargs)

      
      Get the drift function :math:`\tilde{f}` for the SDE.

      See arXiv:2410.02217. Also known as the "drift" term.















      ..
          !! processed by numpydoc !!


   .. py:method:: get_g_tilde()

      
      Get the diffusion function :math:`\tilde{g}` for the SDE.

      See arXiv:2410.02217. Also known as the "diffusion" term.
      Must return a ``(flat_dim, flat_dim)`` matrix for ``ControlTerm``.















      ..
          !! processed by numpydoc !!


   .. py:attribute:: alpha


.. py:class:: ZeroEndsSolver(velocity_model, mu0, sigma0, alpha, eps0 = 0.001)

   Bases: :py:obj:`BaseFmSDESolver`


   
   ZeroEnds SDE solver for flow matching.

   From Tab. 1 of `arXiv:2410.02217 <http://arxiv.org/abs/2410.02217>`_,
   with change of variable for time: t -> 1-t to match flow matching time notation.

   :param velocity_model: Velocity field model.
   :type velocity_model: ModelWrapper
   :param mu0: Prior mean, shape ``(features, channels)``.
   :type mu0: Array
   :param sigma0: Prior std, shape ``(features, channels)``.
   :type sigma0: Array
   :param alpha: Diffusion strength parameter.
   :type alpha: float
   :param eps0: Minimum time value.
   :type eps0: float















   ..
       !! processed by numpydoc !!

   .. py:method:: get_f_tilde(**kwargs)

      
      Get the drift function :math:`\tilde{f}` for the SDE.

      See arXiv:2410.02217. Also known as the "drift" term.















      ..
          !! processed by numpydoc !!


   .. py:method:: get_g_tilde()

      
      Get the diffusion function :math:`\tilde{g}` for the SDE.

      See arXiv:2410.02217. Also known as the "diffusion" term.
      Must return a ``(flat_dim, flat_dim)`` matrix for ``ControlTerm``.















      ..
          !! processed by numpydoc !!


   .. py:attribute:: alpha


