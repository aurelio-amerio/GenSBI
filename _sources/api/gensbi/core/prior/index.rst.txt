gensbi.core.prior
=================

.. py:module:: gensbi.core.prior

.. autoapi-nested-parse::

   Gaussian prior factory for GenSBI pipelines.

   ..
       !! processed by numpydoc !!


Functions
---------

.. autoapisummary::

   gensbi.core.prior.is_gaussian_prior
   gensbi.core.prior.make_gaussian_prior


Module Contents
---------------

.. py:function:: is_gaussian_prior(prior)

   
   Check whether a prior is a Gaussian (Normal-based Independent).

   :param prior: The prior to check.
   :type prior: numpyro.distributions.Distribution

   :returns: True if the prior is ``Independent(Normal(...), ...)``.
   :rtype: bool















   ..
       !! processed by numpydoc !!

.. py:function:: make_gaussian_prior(dim, ch=None, *, mu=0.0, sigma=1.0)

   
   Create a Gaussian prior as a numpyro distribution.

   Two call forms:

   - ``make_gaussian_prior(dim, ch)`` — legacy rank-2 form,
     ``event_shape=(dim, ch)``.
   - ``make_gaussian_prior(event_shape)`` — a single tuple of any rank,
     e.g. ``(H, W, C)`` for pixel-space fields.

   ``mu`` and ``sigma`` are keyword-only: ``make_gaussian_prior(H, W, C)``
   raises ``TypeError`` instead of silently reading ``C`` as the mean.

   :returns: ``Independent(Normal(loc, scale), len(event_shape))``.
   :rtype: dist.Independent















   ..
       !! processed by numpydoc !!

