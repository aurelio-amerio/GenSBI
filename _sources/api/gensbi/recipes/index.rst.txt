gensbi.recipes
==============

.. py:module:: gensbi.recipes

.. autoapi-nested-parse::

   
   Cookie cutter modules for creating and training SBI models.
















   ..
       !! processed by numpydoc !!


Submodules
----------

.. toctree::
   :maxdepth: 1

   /api/gensbi/recipes/conditional_pipeline/index
   /api/gensbi/recipes/flow_pipeline/index
   /api/gensbi/recipes/flux1/index
   /api/gensbi/recipes/flux1joint/index
   /api/gensbi/recipes/id_strategies/index
   /api/gensbi/recipes/joint_pipeline/index
   /api/gensbi/recipes/pipeline/index
   /api/gensbi/recipes/simformer/index
   /api/gensbi/recipes/unconditional_pipeline/index
   /api/gensbi/recipes/utils/index


Classes
-------

.. autoapisummary::

   gensbi.recipes.ConditionalFlowPipeline
   gensbi.recipes.ConditionalPipeline
   gensbi.recipes.Flux1DiffusionPipeline
   gensbi.recipes.Flux1FlowPipeline
   gensbi.recipes.Flux1JointDiffusionPipeline
   gensbi.recipes.Flux1JointFlowPipeline
   gensbi.recipes.Flux1JointSMPipeline
   gensbi.recipes.Flux1SMPipeline
   gensbi.recipes.HealpixRope
   gensbi.recipes.IdStrategy
   gensbi.recipes.JointPipeline
   gensbi.recipes.SimformerDiffusionPipeline
   gensbi.recipes.SimformerFlowPipeline
   gensbi.recipes.SimformerSMPipeline
   gensbi.recipes.UnconditionalPipeline


Package Contents
----------------

.. py:class:: ConditionalFlowPipeline(model, train_dataset, val_dataset, dim_obs, dim_cond, ch_obs=1, ch_cond=1, params=None, training_config=None, structured_obs=False, structured_cond=False)

   Bases: :py:obj:`gensbi.recipes.pipeline.AbstractPipeline`


   
   Max-likelihood NPE pipeline wrapping an ``MAFlow``.

   :param model: A pre-built flow (e.g. ``MAFlow(MAFlowParams(rngs=rngs, dim=dim_obs, cond_dim=dim_cond))``).
   :type model: MAFlow
   :param train_dataset: Yield ``(obs, cond)`` batches.  Shape is ``(B, dim, C)`` for each
                         variable (``C = 1`` for the tabular path; see ``ch_obs``/``ch_cond``).
   :type train_dataset: iterable
   :param val_dataset: Yield ``(obs, cond)`` batches.  Shape is ``(B, dim, C)`` for each
                       variable (``C = 1`` for the tabular path; see ``ch_obs``/``ch_cond``).
   :type val_dataset: iterable
   :param dim_obs:
   :type dim_obs: int
   :param dim_cond:
   :type dim_cond: int
   :param ch_obs: Channel count for the obs and cond variables.  Default 1 (tabular SBI).
                  Values > 1 enable the ``(B, dim, C)`` channel-passthrough path: the
                  channel axis is preserved and forwarded to the flow unchanged (the flow
                  must be built with matching ``channels``/``cond_channels`` in
                  :class:`~gensbi.models.MAFlowParams`).
   :type ch_obs: int, optional
   :param ch_cond: Channel count for the obs and cond variables.  Default 1 (tabular SBI).
                   Values > 1 enable the ``(B, dim, C)`` channel-passthrough path: the
                   channel axis is preserved and forwarded to the flow unchanged (the flow
                   must be built with matching ``channels``/``cond_channels`` in
                   :class:`~gensbi.models.MAFlowParams`).
   :type ch_cond: int, optional
   :param structured_obs: If ``True``, the modeled variable / condition keeps its native
                          structured shape (the model owns it) instead of the tabular
                          ``(B, dim, 1)`` layout. Default ``False``.
   :type structured_obs: bool, optional
   :param structured_cond: If ``True``, the modeled variable / condition keeps its native
                           structured shape (the model owns it) instead of the tabular
                           ``(B, dim, 1)`` layout. Default ``False``.
   :type structured_cond: bool, optional

   .. rubric:: Notes

   Every single-observation method (:meth:`sample`, :meth:`log_prob`,
   :meth:`get_sampler`, :meth:`get_log_prob_fn`) expects ``x_o`` to carry a
   **leading batch axis** (size 1 for one observation) **and a channel axis**:
   shape ``(1, dim_cond, C)`` for tabular, or ``(1,) + per_obs_shape`` for
   structured. A bare ``(B, dim)`` tensor is rejected — add ``[..., None]``
   for ``C = 1``. A batch axis > 1 raises ``ValueError`` — pass a batch to
   :meth:`sample_batched` instead.















   ..
       !! processed by numpydoc !!

   .. py:method:: _make_model(params)
      :abstractmethod:


      
      Create and return the model to be trained.
















      ..
          !! processed by numpydoc !!


   .. py:method:: _prep_cond(x)


   .. py:method:: _prep_obs(x)


   .. py:method:: _wrap_model()

      
      Wrap the model for evaluation (either using JointWrapper or ConditionalWrapper).
















      ..
          !! processed by numpydoc !!


   .. py:method:: fit_standardization(obs_data, axis=0)

      
      Fit the Standardize bijection buffers from training observations.

      Computes per-dimension mean and standard deviation of ``obs_data``
      and stores them as buffers on both the live model and the EMA model.
      EMA only averages ``Param`` variables, so the non-Param buffers must
      be set explicitly here.  Must be called before :meth:`train` when
      input standardization is desired.

      :param obs_data: Training observations of shape ``(N, dim_obs)`` or
                       ``(N, dim_obs, 1)`` (the autoregressive target; e.g. theta for
                       NPE).  For multichannel flows (``ch_obs > 1``) the shape is
                       ``(N, dim_obs, C)`` and ``axis=(0, 1)`` yields per-channel stats.
      :type obs_data: Array
      :param axis: Reduction axis or axes for the mean/std computation.  Default
                   is ``0`` (per-dimension stats over the batch), which is the
                   correct choice for the tabular (``C == 1``) path.  Pass
                   ``axis=(0, 1)`` for per-channel standardization when ``C > 1``.
      :type axis: int or tuple of int, optional















      ..
          !! processed by numpydoc !!


   .. py:method:: get_default_params(*args, **kwargs)
      :classmethod:

      :abstractmethod:


      
      Not implemented: the flow pipeline takes a pre-built ``MAFlow``.

      :raises NotImplementedError: Always. There are no default model params to return; construct an
          ``MAFlow`` directly and pass it as ``model=``.















      ..
          !! processed by numpydoc !!


   .. py:method:: get_log_prob_fn(x_o, use_ema=True, **kwargs)

      
      Return a log-probability closure for a single conditioning observation.

      :param x_o: Single conditioning observation carrying a leading batch axis of
                  size 1 (see :meth:`get_sampler` for the shape convention). A leading
                  batch axis > 1 raises ``ValueError``.
      :type x_o: Array
      :param use_ema: If ``True`` (default), use the EMA model.
      :type use_ema: bool, optional

      :returns: **log_prob_fn** -- A function ``(x_1) -> Array`` of shape ``(B,)`` evaluating
                the conditional log-probability ``log q(x_1 | x_o)`` for a
                batch of ``B`` parameter vectors.  ``x_1`` has shape
                ``(B, dim_obs)`` or ``(B, dim_obs, 1)`` on the tabular path,
                or ``(B, dim_obs, C)`` when ``ch_obs > 1``
                (channel-passthrough).
      :rtype: Callable















      ..
          !! processed by numpydoc !!


   .. py:method:: get_loss_fn()

      
      Return the max-likelihood loss function for training.

      Returns a closure ``loss_fn(model, batch, key) -> Array`` that
      computes the mean negative log-likelihood
      ``-mean(log q(obs | cond))``.  ``batch = (obs, cond)`` with each
      element of shape ``(B, dim, 1)``.  NPE convention: ``obs = theta``,
      ``cond = x``.  The ``key`` argument is accepted for interface
      compatibility but is unused.

      :returns: **loss_fn** -- A function ``(model, batch, key) -> Array`` returning the scalar
                mean negative log-likelihood.
      :rtype: Callable















      ..
          !! processed by numpydoc !!


   .. py:method:: get_sampler(x_o, use_ema=True, **kwargs)

      
      Return a sampler closure for a single conditioning observation.

      :param x_o: Single conditioning observation.  Must carry a leading batch axis
                  and a channel axis for tabular cond: shape ``(1, dim_cond, C)``.
                  For structured cond: ``(1,) + per_observation_shape``.
                  A leading batch axis > 1 raises ``ValueError`` (use
                  :meth:`sample_batched` for many conditions).
      :type x_o: Array
      :param use_ema: If ``True`` (default), use the EMA model; otherwise use the
                      live model.
      :type use_ema: bool, optional

      :returns: **sampler** -- A function ``(key, nsamples) -> Array`` returning the model's
                native output shape ``(nsamples, dim_obs, C)`` (channel always
                carried).
      :rtype: Callable















      ..
          !! processed by numpydoc !!


   .. py:method:: init_pipeline_from_config(*args, **kwargs)
      :classmethod:

      :abstractmethod:


      
      Not implemented: the flow pipeline requires a pre-built model.

      :raises NotImplementedError: Always. Construct an ``MAFlow`` and pass it as ``model=`` to the
          pipeline constructor instead.















      ..
          !! processed by numpydoc !!


   .. py:method:: log_prob(x_1, x_o, use_ema=True, **kwargs)

      
      Evaluate the conditional log-probability for a batch of samples.

      :param x_1: Batch of parameter vectors of shape ``(B, dim_obs)`` or
                  ``(B, dim_obs, 1)``.
      :type x_1: Array
      :param x_o: Single conditioning observation carrying a leading batch axis of
                  size 1 (see :meth:`get_sampler` for the shape convention). A leading
                  batch axis > 1 raises ``ValueError``.
      :type x_o: Array
      :param use_ema: If ``True`` (default), use the EMA model.
      :type use_ema: bool, optional

      :returns: **log_prob** -- Log-probabilities of shape ``(B,)``.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:method:: sample(key, x_o, nsamples=10000, use_ema=True, **kwargs)

      
      Draw posterior samples for a single conditioning observation.

      :param key: Random key.
      :type key: jax.random.PRNGKey
      :param x_o: Single conditioning observation carrying a leading batch axis of
                  size 1 (see :meth:`get_sampler` for the shape convention). A leading
                  batch axis > 1 raises ``ValueError``.
      :type x_o: Array
      :param nsamples: Number of posterior samples to draw.  Default is 10 000.
      :type nsamples: int, optional
      :param use_ema: If ``True`` (default), use the EMA model.
      :type use_ema: bool, optional

      :returns: **samples** -- Posterior samples of shape ``(nsamples, dim_obs, 1)`` for the
                tabular default (``C = 1``), or ``(nsamples, dim_obs, C)`` for
                ``ch_obs = C`` — the channel axis is always carried for a
                vector-modeled variable regardless of ``structured_cond`` (a
                structured condition changes only ``x_o``'s expected shape, not
                the modeled variable's). When ``structured_obs=True``, samples
                instead have shape ``(nsamples,) + per_obs_shape``, the model's
                native structured output.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:method:: sample_batched(key, x_o, nsamples=10000, *, use_ema=True, **kwargs)

      
      Draw posterior samples for a batch of conditioning observations.

      Draws all ``B`` conditions in **one** batched autoregressive pass:
      each condition is repeated ``nsamples`` times and concatenated into
      a single ``(B * nsamples, ...)`` batch fed to the flow's ``sample``,
      rather than looping the single-observation sampler ``B`` times.
      Memory scales with the product ``B * nsamples``.

      :param key: Random key for the batched sampling pass.
      :type key: jax.random.PRNGKey
      :param x_o: Batch of observations. For tabular cond: shape
                  ``(B, dim_cond, C)`` (a bare ``(B, dim_cond)`` raises
                  ``ValueError`` — add a trailing channel axis). For structured
                  cond: ``(B,) + per_obs_shape``.
      :type x_o: Array
      :param nsamples: Number of posterior samples per observation.  Default is
                       10 000.
      :type nsamples: int, optional
      :param use_ema: If ``True`` (default), use the EMA model.
      :type use_ema: bool, optional
      :param \*\*kwargs: Extra keyword arguments accepted for interface compatibility
                         and silently ignored (e.g. ``chunk_size`` and
                         ``show_progress_bars`` from
                         :class:`~gensbi.recipes.pipeline.AbstractPipeline`).
      :type \*\*kwargs: dict, optional

      :returns: **samples** -- Posterior samples of shape ``(nsamples, B, dim_obs, 1)`` for the
                tabular default (``C = 1``), or ``(nsamples, B, dim_obs, C)`` for
                ``ch_obs = C`` — the channel axis is always carried for a
                vector-modeled variable regardless of ``structured_cond`` (a
                structured condition changes only ``x_o``'s expected shape, not
                the modeled variable's). When ``structured_obs=True``, samples
                instead have shape ``(nsamples, B) + per_obs_shape``, the
                model's native structured output. In both cases ``out[:, i]``
                is the samples for condition ``i`` — the same layout as the
                previous per-condition loop.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:method:: train(rngs, nsteps=None, save_model=True)

      
      Train the flow model, warning if standardization was skipped.

      Delegates to :meth:`AbstractPipeline.train` after checking that
      :meth:`fit_standardization` was called.

      :param rngs: Random number generators for training and validation steps.
      :type rngs: nnx.Rngs
      :param nsteps: Number of training steps.  If ``None``, taken from
                     ``training_config["nsteps"]``.  Default is ``None``.
      :type nsteps: int or None, optional
      :param save_model: If ``True`` (default), serialise the model to disk after
                         training.
      :type save_model: bool, optional

      :returns: * **loss_array** (*list*) -- Per-step training losses.
                * **val_loss_array** (*list*) -- Validation losses recorded at each validation checkpoint.















      ..
          !! processed by numpydoc !!


   .. py:attribute:: _standardized
      :value: False



   .. py:attribute:: structured_cond
      :value: False



   .. py:attribute:: structured_obs
      :value: False



.. py:class:: ConditionalPipeline(model, train_dataset, val_dataset, dim_obs, dim_cond, method, ch_obs=1, ch_cond=1, id_embedding_strategy=('absolute', 'absolute'), size=2, params=None, training_config=None)

   Bases: :py:obj:`gensbi.recipes.pipeline.AbstractPipeline`


   
   Model-agnostic conditional pipeline parameterized by a ``GenerativeMethod``.

   Unlike the old method-specific pipeline classes, this class
   works with **any** generative method and **any** user-provided model that
   conforms to the ``ConditionalWrapper`` interface.

   :param model: The model to be trained.
   :type model: nnx.Module
   :param train_dataset: Training dataset yielding ``(obs, cond)`` batches.
   :type train_dataset: iterable
   :param val_dataset: Validation dataset yielding ``(obs, cond)`` batches.
   :type val_dataset: iterable
   :param dim_obs: Dimension of the observation/parameter space.
   :type dim_obs: int or tuple of int
   :param dim_cond: Dimension of the conditioning space.
   :type dim_cond: int or tuple of int
   :param method: Strategy object (e.g. ``FlowMatchingMethod()``,
                  ``DiffusionEDMMethod()``, ``ScoreMatchingMethod()``).
   :type method: GenerativeMethod
   :param ch_obs: Number of channels per observation token. Default is 1.
   :type ch_obs: int, optional
   :param ch_cond: Number of channels per conditioning token. Default is 1.
   :type ch_cond: int, optional
   :param id_embedding_strategy: Per-stream (obs, cond) id-builder strategy. Strings pick a built-in
                                 1D/2D grid builder; an :class:`~gensbi.recipes.IdStrategy` object
                                 (e.g. :class:`~gensbi.recipes.HealpixRope`) builds ids from its own
                                 geometry. Default is ``("absolute", "absolute")``. NOTE this
                                 pipeline-side vocabulary is distinct from the model-side
                                 ``id_embedding_strategy`` (e.g. ``Flux1Params``), where "rope"
                                 means "apply RoPE to the provided ids"; a ``HealpixRope`` pipeline
                                 strategy pairs with model-side ``("absolute", "rope")`` and a
                                 3-entry ``axes_dim``.
   :type id_embedding_strategy: tuple of (str or IdStrategy), optional
   :param size: Patch edge length for 2D ID-embedding strategies. Default is ``2``.
                A single ``int`` is broadcast to both obs and cond (``8 -> (8, 8)``).
                A length-2 tuple ``(obs_size, cond_size)`` lets the two inputs differ.
                Use ``1`` to disable patchification for an input. Ignored for 1D
                strategies (``"absolute"``, ``"pos1d"``, ``"rope1d"``).
   :type size: int or tuple of int, optional
   :param params: Model parameters (stored but not used directly).
   :type params: optional
   :param training_config: Training configuration. If ``None``, uses defaults augmented
                           by ``method.get_extra_training_config()``.
   :type training_config: dict, optional

   .. rubric:: Examples

   >>> from gensbi.core import FlowMatchingMethod
   >>> pipeline = ConditionalPipeline(
   ...     model=my_model,
   ...     train_dataset=train_ds,
   ...     val_dataset=val_ds,
   ...     dim_obs=5, dim_cond=3,
   ...     method=FlowMatchingMethod(),
   ... )















   ..
       !! processed by numpydoc !!

   .. py:method:: _make_model()
      :abstractmethod:


      
      Create and return the model to be trained.
















      ..
          !! processed by numpydoc !!


   .. py:method:: _wrap_model()

      
      Wrap the model for evaluation (either using JointWrapper or ConditionalWrapper).
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_default_params(*args, **kwargs)
      :classmethod:

      :abstractmethod:



   .. py:method:: get_log_prob_fn(x_o, use_ema=True, model_extras=None, **kwargs)

      
      Get a log-probability function.

      :param x_o: Conditioning variable (observed data).
      :type x_o: array-like
      :param use_ema: Whether to use the EMA model. Default is True.
      :type use_ema: bool, optional
      :param model_extras: Additional model extras. Cannot override protected keys.
      :type model_extras: dict, optional
      :param \*\*kwargs: Forwarded to ``method.build_log_prob_fn``.

      :returns: ``log_prob_fn(x_1) -> log_prob``
      :rtype: Callable















      ..
          !! processed by numpydoc !!


   .. py:method:: get_loss_fn()

      
      Return the loss function for training/validation.
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_sampler(x_o, use_ema=True, model_extras=None, **sampler_kwargs)

      
      Get a sampler function.

      :param x_o: Conditioning variable (observed data).
      :type x_o: array-like
      :param use_ema: Whether to use the EMA model. Default is True.
      :type use_ema: bool, optional
      :param model_extras: Additional keyword arguments passed to the model during sampling
                           (e.g. ``{"edge_mask": mask}``). Cannot override the protected keys
                           ``cond``, ``obs_ids``, ``cond_ids``.
      :type model_extras: dict, optional
      :param \*\*sampler_kwargs: Forwarded to ``method.build_sampler_fn`` (e.g. ``step_size``,
                                 ``nsteps``, ``solver``, ``time_grid``).

      :returns: ``sampler(key, nsamples) -> samples``
      :rtype: Callable















      ..
          !! processed by numpydoc !!


   .. py:method:: init_pipeline_from_config(*args, **kwargs)
      :classmethod:

      :abstractmethod:


      
      Initialize the pipeline from a configuration file.

      :param train_dataset: Training dataset.
      :type train_dataset: iterable
      :param val_dataset: Validation dataset.
      :type val_dataset: iterable
      :param dim_obs: Dimensionality of the parameter (theta) space.
      :type dim_obs: int
      :param dim_cond: Dimensionality of the observation (x) space.
      :type dim_cond: int
      :param config_path: Path to the configuration file.
      :type config_path: str
      :param checkpoint_dir: Directory for saving checkpoints.
      :type checkpoint_dir: str

      :returns: **pipeline** -- An instance of the pipeline initialized from the configuration.
      :rtype: AbstractPipeline















      ..
          !! processed by numpydoc !!


   .. py:method:: log_prob(x_1, x_o, use_ema=True, *, key=None, **kwargs)

      
      Compute log-probability of x_1 given x_o.

      :param x_1: Data samples to evaluate.
      :type x_1: array-like
      :param x_o: Conditioning variable.
      :type x_o: array-like
      :param use_ema: Use the EMA model. Default is True.
      :type use_ema: bool, optional
      :param key: Required when ``exact_divergence=False`` (Hutchinson).
      :type key: jax.random.PRNGKey, optional
      :param \*\*kwargs: Forwarded to :meth:`get_log_prob_fn`.

      :returns: Log-probabilities.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:method:: sample(key, x_o, nsamples=10000, use_ema=True, **sampler_kwargs)

      
      Draw samples from the model.

      :param key: Random key.
      :type key: jax.random.PRNGKey
      :param x_o: Conditioning variable.
      :type x_o: array-like
      :param nsamples: Number of samples. Default is 10 000.
      :type nsamples: int, optional
      :param use_ema: Use the EMA model. Default is True.
      :type use_ema: bool, optional
      :param \*\*sampler_kwargs: Forwarded to :meth:`get_sampler`.

      :returns: Samples of shape ``(nsamples, dim_obs, ch_obs)``.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:attribute:: loss_obj


   .. py:attribute:: method


   .. py:attribute:: path


.. py:class:: Flux1DiffusionPipeline(train_dataset, val_dataset, dim_obs, dim_cond, ch_obs=1, ch_cond=1, params=None, training_config=None)

   Bases: :py:obj:`gensbi.recipes.conditional_pipeline.ConditionalPipeline`


   
   Model-agnostic conditional pipeline parameterized by a ``GenerativeMethod``.

   Unlike the old method-specific pipeline classes, this class
   works with **any** generative method and **any** user-provided model that
   conforms to the ``ConditionalWrapper`` interface.

   :param model: The model to be trained.
   :type model: nnx.Module
   :param train_dataset: Training dataset yielding ``(obs, cond)`` batches.
   :type train_dataset: iterable
   :param val_dataset: Validation dataset yielding ``(obs, cond)`` batches.
   :type val_dataset: iterable
   :param dim_obs: Dimension of the observation/parameter space.
   :type dim_obs: int or tuple of int
   :param dim_cond: Dimension of the conditioning space.
   :type dim_cond: int or tuple of int
   :param method: Strategy object (e.g. ``FlowMatchingMethod()``,
                  ``DiffusionEDMMethod()``, ``ScoreMatchingMethod()``).
   :type method: GenerativeMethod
   :param ch_obs: Number of channels per observation token. Default is 1.
   :type ch_obs: int, optional
   :param ch_cond: Number of channels per conditioning token. Default is 1.
   :type ch_cond: int, optional
   :param id_embedding_strategy: Per-stream (obs, cond) id-builder strategy. Strings pick a built-in
                                 1D/2D grid builder; an :class:`~gensbi.recipes.IdStrategy` object
                                 (e.g. :class:`~gensbi.recipes.HealpixRope`) builds ids from its own
                                 geometry. Default is ``("absolute", "absolute")``. NOTE this
                                 pipeline-side vocabulary is distinct from the model-side
                                 ``id_embedding_strategy`` (e.g. ``Flux1Params``), where "rope"
                                 means "apply RoPE to the provided ids"; a ``HealpixRope`` pipeline
                                 strategy pairs with model-side ``("absolute", "rope")`` and a
                                 3-entry ``axes_dim``.
   :type id_embedding_strategy: tuple of (str or IdStrategy), optional
   :param size: Patch edge length for 2D ID-embedding strategies. Default is ``2``.
                A single ``int`` is broadcast to both obs and cond (``8 -> (8, 8)``).
                A length-2 tuple ``(obs_size, cond_size)`` lets the two inputs differ.
                Use ``1`` to disable patchification for an input. Ignored for 1D
                strategies (``"absolute"``, ``"pos1d"``, ``"rope1d"``).
   :type size: int or tuple of int, optional
   :param params: Model parameters (stored but not used directly).
   :type params: optional
   :param training_config: Training configuration. If ``None``, uses defaults augmented
                           by ``method.get_extra_training_config()``.
   :type training_config: dict, optional

   .. rubric:: Examples

   >>> from gensbi.core import FlowMatchingMethod
   >>> pipeline = ConditionalPipeline(
   ...     model=my_model,
   ...     train_dataset=train_ds,
   ...     val_dataset=val_ds,
   ...     dim_obs=5, dim_cond=3,
   ...     method=FlowMatchingMethod(),
   ... )















   ..
       !! processed by numpydoc !!

   .. py:method:: _make_model(params)

      
      Create and return the Flux1 model to be trained.
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_default_params(dim_obs, dim_cond, ch_obs, ch_cond)
      :classmethod:


      
      Return a dictionary of default model parameters.
















      ..
          !! processed by numpydoc !!


   .. py:method:: init_pipeline_from_config(train_dataset, val_dataset, dim_obs, dim_cond, config_path, checkpoint_dir, **kwargs)
      :classmethod:


      
      Initialize the pipeline from a configuration file.

      :param config_path: Path to the configuration file.
      :type config_path: str
      :param \*\*kwargs: Additional keyword arguments forwarded to the constructor.















      ..
          !! processed by numpydoc !!


   .. py:attribute:: ema_model


.. py:class:: Flux1FlowPipeline(train_dataset, val_dataset, dim_obs, dim_cond, ch_obs=1, ch_cond=1, params=None, training_config=None)

   Bases: :py:obj:`gensbi.recipes.conditional_pipeline.ConditionalPipeline`


   
   Model-agnostic conditional pipeline parameterized by a ``GenerativeMethod``.

   Unlike the old method-specific pipeline classes, this class
   works with **any** generative method and **any** user-provided model that
   conforms to the ``ConditionalWrapper`` interface.

   :param model: The model to be trained.
   :type model: nnx.Module
   :param train_dataset: Training dataset yielding ``(obs, cond)`` batches.
   :type train_dataset: iterable
   :param val_dataset: Validation dataset yielding ``(obs, cond)`` batches.
   :type val_dataset: iterable
   :param dim_obs: Dimension of the observation/parameter space.
   :type dim_obs: int or tuple of int
   :param dim_cond: Dimension of the conditioning space.
   :type dim_cond: int or tuple of int
   :param method: Strategy object (e.g. ``FlowMatchingMethod()``,
                  ``DiffusionEDMMethod()``, ``ScoreMatchingMethod()``).
   :type method: GenerativeMethod
   :param ch_obs: Number of channels per observation token. Default is 1.
   :type ch_obs: int, optional
   :param ch_cond: Number of channels per conditioning token. Default is 1.
   :type ch_cond: int, optional
   :param id_embedding_strategy: Per-stream (obs, cond) id-builder strategy. Strings pick a built-in
                                 1D/2D grid builder; an :class:`~gensbi.recipes.IdStrategy` object
                                 (e.g. :class:`~gensbi.recipes.HealpixRope`) builds ids from its own
                                 geometry. Default is ``("absolute", "absolute")``. NOTE this
                                 pipeline-side vocabulary is distinct from the model-side
                                 ``id_embedding_strategy`` (e.g. ``Flux1Params``), where "rope"
                                 means "apply RoPE to the provided ids"; a ``HealpixRope`` pipeline
                                 strategy pairs with model-side ``("absolute", "rope")`` and a
                                 3-entry ``axes_dim``.
   :type id_embedding_strategy: tuple of (str or IdStrategy), optional
   :param size: Patch edge length for 2D ID-embedding strategies. Default is ``2``.
                A single ``int`` is broadcast to both obs and cond (``8 -> (8, 8)``).
                A length-2 tuple ``(obs_size, cond_size)`` lets the two inputs differ.
                Use ``1`` to disable patchification for an input. Ignored for 1D
                strategies (``"absolute"``, ``"pos1d"``, ``"rope1d"``).
   :type size: int or tuple of int, optional
   :param params: Model parameters (stored but not used directly).
   :type params: optional
   :param training_config: Training configuration. If ``None``, uses defaults augmented
                           by ``method.get_extra_training_config()``.
   :type training_config: dict, optional

   .. rubric:: Examples

   >>> from gensbi.core import FlowMatchingMethod
   >>> pipeline = ConditionalPipeline(
   ...     model=my_model,
   ...     train_dataset=train_ds,
   ...     val_dataset=val_ds,
   ...     dim_obs=5, dim_cond=3,
   ...     method=FlowMatchingMethod(),
   ... )















   ..
       !! processed by numpydoc !!

   .. py:method:: _make_model(params)

      
      Create and return the Flux1 model to be trained.
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_default_params(dim_obs, dim_cond, ch_obs, ch_cond)
      :classmethod:


      
      Return a dictionary of default model parameters.
















      ..
          !! processed by numpydoc !!


   .. py:method:: init_pipeline_from_config(train_dataset, val_dataset, dim_obs, dim_cond, config_path, checkpoint_dir, **kwargs)
      :classmethod:


      
      Initialize the pipeline from a configuration file.

      :param config_path: Path to the configuration file.
      :type config_path: str
      :param \*\*kwargs: Additional keyword arguments forwarded to the constructor.















      ..
          !! processed by numpydoc !!


   .. py:attribute:: ema_model


.. py:class:: Flux1JointDiffusionPipeline(train_dataset, val_dataset, dim_obs, dim_cond, ch_obs = 1, params=None, training_config=None, condition_mask_kind='structured')

   Bases: :py:obj:`gensbi.recipes.joint_pipeline.JointPipeline`


   
   Model-agnostic joint pipeline parameterized by a ``GenerativeMethod``.

   Unlike the old method-specific pipeline classes, this class works with
   **any** generative method and **any** user-provided model that conforms
   to the ``JointWrapper`` interface.

   :param model: The model to be trained.
   :type model: nnx.Module
   :param train_dataset: Training dataset yielding concatenated ``x_1`` batches
                         (obs and cond concatenated along the token dimension).
   :type train_dataset: iterable
   :param val_dataset: Validation dataset.
   :type val_dataset: iterable
   :param dim_obs: Dimension of the observation/parameter space.
   :type dim_obs: int
   :param dim_cond: Dimension of the conditioning space.
   :type dim_cond: int
   :param method: Strategy object (e.g. ``FlowMatchingMethod()``,
                  ``DiffusionEDMMethod()``, ``ScoreMatchingMethod()``).
   :type method: GenerativeMethod
   :param ch_obs: Number of channels per token. Default is 1.
   :type ch_obs: int, optional
   :param condition_mask_kind: Kind of condition mask. One of ``"structured"`` or ``"posterior"``.
                               Default is ``"structured"``.
   :type condition_mask_kind: str, optional
   :param params: Model parameters (stored but not used directly).
   :type params: optional
   :param training_config: Training configuration.
   :type training_config: dict, optional

   .. rubric:: Examples

   >>> from gensbi.core import FlowMatchingMethod
   >>> pipeline = JointPipeline(
   ...     model=my_model,
   ...     train_dataset=train_ds,
   ...     val_dataset=val_ds,
   ...     dim_obs=2, dim_cond=7,
   ...     method=FlowMatchingMethod(),
   ... )















   ..
       !! processed by numpydoc !!

   .. py:method:: _make_model(params)

      
      Create and return the Flux1Joint model to be trained.
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_default_params(dim_joint, in_channels)
      :classmethod:


      
      Return a dictionary of default model parameters.
















      ..
          !! processed by numpydoc !!


   .. py:method:: init_pipeline_from_config(train_dataset, val_dataset, dim_obs, dim_cond, config_path, checkpoint_dir, **kwargs)
      :classmethod:


      
      Initialize the pipeline from a configuration file.

      :param config_path: Path to the configuration file.
      :type config_path: str
      :param \*\*kwargs: Additional keyword arguments forwarded to the constructor.















      ..
          !! processed by numpydoc !!


   .. py:attribute:: ch_obs
      :value: 1



   .. py:attribute:: dim_joint


   .. py:attribute:: ema_model


.. py:class:: Flux1JointFlowPipeline(train_dataset, val_dataset, dim_obs, dim_cond, ch_obs = 1, params=None, training_config=None, condition_mask_kind='structured')

   Bases: :py:obj:`gensbi.recipes.joint_pipeline.JointPipeline`


   
   Model-agnostic joint pipeline parameterized by a ``GenerativeMethod``.

   Unlike the old method-specific pipeline classes, this class works with
   **any** generative method and **any** user-provided model that conforms
   to the ``JointWrapper`` interface.

   :param model: The model to be trained.
   :type model: nnx.Module
   :param train_dataset: Training dataset yielding concatenated ``x_1`` batches
                         (obs and cond concatenated along the token dimension).
   :type train_dataset: iterable
   :param val_dataset: Validation dataset.
   :type val_dataset: iterable
   :param dim_obs: Dimension of the observation/parameter space.
   :type dim_obs: int
   :param dim_cond: Dimension of the conditioning space.
   :type dim_cond: int
   :param method: Strategy object (e.g. ``FlowMatchingMethod()``,
                  ``DiffusionEDMMethod()``, ``ScoreMatchingMethod()``).
   :type method: GenerativeMethod
   :param ch_obs: Number of channels per token. Default is 1.
   :type ch_obs: int, optional
   :param condition_mask_kind: Kind of condition mask. One of ``"structured"`` or ``"posterior"``.
                               Default is ``"structured"``.
   :type condition_mask_kind: str, optional
   :param params: Model parameters (stored but not used directly).
   :type params: optional
   :param training_config: Training configuration.
   :type training_config: dict, optional

   .. rubric:: Examples

   >>> from gensbi.core import FlowMatchingMethod
   >>> pipeline = JointPipeline(
   ...     model=my_model,
   ...     train_dataset=train_ds,
   ...     val_dataset=val_ds,
   ...     dim_obs=2, dim_cond=7,
   ...     method=FlowMatchingMethod(),
   ... )















   ..
       !! processed by numpydoc !!

   .. py:method:: _make_model(params)

      
      Create and return the Flux1Joint model to be trained.
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_default_params(dim_joint, in_channels)
      :classmethod:


      
      Return a dictionary of default model parameters.
















      ..
          !! processed by numpydoc !!


   .. py:method:: init_pipeline_from_config(train_dataset, val_dataset, dim_obs, dim_cond, config_path, checkpoint_dir, **kwargs)
      :classmethod:


      
      Initialize the pipeline from a configuration file.

      :param config_path: Path to the configuration file.
      :type config_path: str
      :param \*\*kwargs: Additional keyword arguments forwarded to the constructor.















      ..
          !! processed by numpydoc !!


   .. py:attribute:: ch_obs
      :value: 1



   .. py:attribute:: dim_joint


   .. py:attribute:: ema_model


.. py:class:: Flux1JointSMPipeline(train_dataset, val_dataset, dim_obs, dim_cond, ch_obs = 1, sde_type = 'VP', params=None, training_config=None, condition_mask_kind='structured')

   Bases: :py:obj:`gensbi.recipes.joint_pipeline.JointPipeline`


   
   Model-agnostic joint pipeline parameterized by a ``GenerativeMethod``.

   Unlike the old method-specific pipeline classes, this class works with
   **any** generative method and **any** user-provided model that conforms
   to the ``JointWrapper`` interface.

   :param model: The model to be trained.
   :type model: nnx.Module
   :param train_dataset: Training dataset yielding concatenated ``x_1`` batches
                         (obs and cond concatenated along the token dimension).
   :type train_dataset: iterable
   :param val_dataset: Validation dataset.
   :type val_dataset: iterable
   :param dim_obs: Dimension of the observation/parameter space.
   :type dim_obs: int
   :param dim_cond: Dimension of the conditioning space.
   :type dim_cond: int
   :param method: Strategy object (e.g. ``FlowMatchingMethod()``,
                  ``DiffusionEDMMethod()``, ``ScoreMatchingMethod()``).
   :type method: GenerativeMethod
   :param ch_obs: Number of channels per token. Default is 1.
   :type ch_obs: int, optional
   :param condition_mask_kind: Kind of condition mask. One of ``"structured"`` or ``"posterior"``.
                               Default is ``"structured"``.
   :type condition_mask_kind: str, optional
   :param params: Model parameters (stored but not used directly).
   :type params: optional
   :param training_config: Training configuration.
   :type training_config: dict, optional

   .. rubric:: Examples

   >>> from gensbi.core import FlowMatchingMethod
   >>> pipeline = JointPipeline(
   ...     model=my_model,
   ...     train_dataset=train_ds,
   ...     val_dataset=val_ds,
   ...     dim_obs=2, dim_cond=7,
   ...     method=FlowMatchingMethod(),
   ... )















   ..
       !! processed by numpydoc !!

   .. py:method:: _make_model(params)

      
      Create and return the Flux1Joint model to be trained.
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_default_params(dim_joint, in_channels)
      :classmethod:


      
      Return a dictionary of default model parameters.
















      ..
          !! processed by numpydoc !!


   .. py:method:: init_pipeline_from_config(train_dataset, val_dataset, dim_obs, dim_cond, config_path, checkpoint_dir, **kwargs)
      :classmethod:


      
      Initialize the pipeline from a configuration file.

      :param config_path: Path to the configuration file.
      :type config_path: str
      :param \*\*kwargs: Additional keyword arguments forwarded to the constructor
                         (e.g. ``sde_type="VE"`` for score matching).















      ..
          !! processed by numpydoc !!


   .. py:attribute:: ch_obs
      :value: 1



   .. py:attribute:: dim_joint


   .. py:attribute:: ema_model


.. py:class:: Flux1SMPipeline(train_dataset, val_dataset, dim_obs, dim_cond, ch_obs=1, ch_cond=1, sde_type = 'VP', params=None, training_config=None)

   Bases: :py:obj:`gensbi.recipes.conditional_pipeline.ConditionalPipeline`


   
   Model-agnostic conditional pipeline parameterized by a ``GenerativeMethod``.

   Unlike the old method-specific pipeline classes, this class
   works with **any** generative method and **any** user-provided model that
   conforms to the ``ConditionalWrapper`` interface.

   :param model: The model to be trained.
   :type model: nnx.Module
   :param train_dataset: Training dataset yielding ``(obs, cond)`` batches.
   :type train_dataset: iterable
   :param val_dataset: Validation dataset yielding ``(obs, cond)`` batches.
   :type val_dataset: iterable
   :param dim_obs: Dimension of the observation/parameter space.
   :type dim_obs: int or tuple of int
   :param dim_cond: Dimension of the conditioning space.
   :type dim_cond: int or tuple of int
   :param method: Strategy object (e.g. ``FlowMatchingMethod()``,
                  ``DiffusionEDMMethod()``, ``ScoreMatchingMethod()``).
   :type method: GenerativeMethod
   :param ch_obs: Number of channels per observation token. Default is 1.
   :type ch_obs: int, optional
   :param ch_cond: Number of channels per conditioning token. Default is 1.
   :type ch_cond: int, optional
   :param id_embedding_strategy: Per-stream (obs, cond) id-builder strategy. Strings pick a built-in
                                 1D/2D grid builder; an :class:`~gensbi.recipes.IdStrategy` object
                                 (e.g. :class:`~gensbi.recipes.HealpixRope`) builds ids from its own
                                 geometry. Default is ``("absolute", "absolute")``. NOTE this
                                 pipeline-side vocabulary is distinct from the model-side
                                 ``id_embedding_strategy`` (e.g. ``Flux1Params``), where "rope"
                                 means "apply RoPE to the provided ids"; a ``HealpixRope`` pipeline
                                 strategy pairs with model-side ``("absolute", "rope")`` and a
                                 3-entry ``axes_dim``.
   :type id_embedding_strategy: tuple of (str or IdStrategy), optional
   :param size: Patch edge length for 2D ID-embedding strategies. Default is ``2``.
                A single ``int`` is broadcast to both obs and cond (``8 -> (8, 8)``).
                A length-2 tuple ``(obs_size, cond_size)`` lets the two inputs differ.
                Use ``1`` to disable patchification for an input. Ignored for 1D
                strategies (``"absolute"``, ``"pos1d"``, ``"rope1d"``).
   :type size: int or tuple of int, optional
   :param params: Model parameters (stored but not used directly).
   :type params: optional
   :param training_config: Training configuration. If ``None``, uses defaults augmented
                           by ``method.get_extra_training_config()``.
   :type training_config: dict, optional

   .. rubric:: Examples

   >>> from gensbi.core import FlowMatchingMethod
   >>> pipeline = ConditionalPipeline(
   ...     model=my_model,
   ...     train_dataset=train_ds,
   ...     val_dataset=val_ds,
   ...     dim_obs=5, dim_cond=3,
   ...     method=FlowMatchingMethod(),
   ... )















   ..
       !! processed by numpydoc !!

   .. py:method:: _make_model(params)

      
      Create and return the Flux1 model to be trained.
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_default_params(dim_obs, dim_cond, ch_obs, ch_cond)
      :classmethod:


      
      Return a dictionary of default model parameters.
















      ..
          !! processed by numpydoc !!


   .. py:method:: init_pipeline_from_config(train_dataset, val_dataset, dim_obs, dim_cond, config_path, checkpoint_dir, **kwargs)
      :classmethod:


      
      Initialize the pipeline from a configuration file.

      :param config_path: Path to the configuration file.
      :type config_path: str
      :param \*\*kwargs: Additional keyword arguments forwarded to the constructor
                         (e.g. ``sde_type="VE"`` for score matching).















      ..
          !! processed by numpydoc !!


   .. py:attribute:: ema_model


.. py:class:: HealpixRope

   
   Spherical RoPE ids for tokens on a HEALPix grid (name: "healpix-rope").

   Wraps :func:`gensbi.recipes.utils.init_ids_healpix` (see there for the
   method and its rationale) with the geometry needed to build the ids —
   which the string-enum API cannot carry, since the pipeline only passes a
   token count.

   :param nside: HEALPix resolution of the *token* grid (power of 2).
   :type nside: int
   :param base_pixels: Base pixels (0..11) covered by the grid; ``None`` = full sky.
   :type base_pixels: sequence of int, optional















   ..
       !! processed by numpydoc !!

   .. py:method:: __post_init__()


   .. py:method:: build(dim)

      
      Return ``(ids, num_tokens)``; ``dim`` must match the grid.
















      ..
          !! processed by numpydoc !!


   .. py:attribute:: base_pixels
      :type:  Optional[Union[Tuple[int, Ellipsis], Sequence[int]]]
      :value: None



   .. py:attribute:: name
      :type:  ClassVar[str]
      :value: 'healpix-rope'



   .. py:attribute:: nside
      :type:  int


   .. py:property:: num_tokens
      :type: int


      
      ``n_faces * nside**2``.

      :type: Token count of the grid















      ..
          !! processed by numpydoc !!


   .. py:property:: theta
      :type: int


      
      :func:`healpix_rope_theta`.

      :type: Suggested model-side RoPE theta















      ..
          !! processed by numpydoc !!


.. py:class:: IdStrategy

   Bases: :py:obj:`Protocol`


   
   Structural interface for pipeline id-builder strategy objects.

   Any object with a ``name`` and a ``build(dim) -> (ids, resolved_dim)``
   method can be passed in an ``id_embedding_strategy`` tuple slot; the
   pipeline calls ``build`` with the corresponding ``dim_obs``/``dim_cond``.
   Strategies own their full geometry — unlike the string strategies they
   receive no ``semantic_id``/``size``.















   ..
       !! processed by numpydoc !!

   .. py:method:: build(dim)

      
      Return ``(ids, resolved_dim)`` for a stream of ``dim`` tokens.
















      ..
          !! processed by numpydoc !!


   .. py:attribute:: name
      :type:  str


.. py:class:: JointPipeline(model, train_dataset, val_dataset, dim_obs, dim_cond, method, ch_obs=1, condition_mask_kind='structured', params=None, training_config=None)

   Bases: :py:obj:`gensbi.recipes.pipeline.AbstractPipeline`


   
   Model-agnostic joint pipeline parameterized by a ``GenerativeMethod``.

   Unlike the old method-specific pipeline classes, this class works with
   **any** generative method and **any** user-provided model that conforms
   to the ``JointWrapper`` interface.

   :param model: The model to be trained.
   :type model: nnx.Module
   :param train_dataset: Training dataset yielding concatenated ``x_1`` batches
                         (obs and cond concatenated along the token dimension).
   :type train_dataset: iterable
   :param val_dataset: Validation dataset.
   :type val_dataset: iterable
   :param dim_obs: Dimension of the observation/parameter space.
   :type dim_obs: int
   :param dim_cond: Dimension of the conditioning space.
   :type dim_cond: int
   :param method: Strategy object (e.g. ``FlowMatchingMethod()``,
                  ``DiffusionEDMMethod()``, ``ScoreMatchingMethod()``).
   :type method: GenerativeMethod
   :param ch_obs: Number of channels per token. Default is 1.
   :type ch_obs: int, optional
   :param condition_mask_kind: Kind of condition mask. One of ``"structured"`` or ``"posterior"``.
                               Default is ``"structured"``.
   :type condition_mask_kind: str, optional
   :param params: Model parameters (stored but not used directly).
   :type params: optional
   :param training_config: Training configuration.
   :type training_config: dict, optional

   .. rubric:: Examples

   >>> from gensbi.core import FlowMatchingMethod
   >>> pipeline = JointPipeline(
   ...     model=my_model,
   ...     train_dataset=train_ds,
   ...     val_dataset=val_ds,
   ...     dim_obs=2, dim_cond=7,
   ...     method=FlowMatchingMethod(),
   ... )















   ..
       !! processed by numpydoc !!

   .. py:method:: _make_model()
      :abstractmethod:


      
      Create and return the model to be trained.
















      ..
          !! processed by numpydoc !!


   .. py:method:: _wrap_model()

      
      Wrap the model for evaluation (either using JointWrapper or ConditionalWrapper).
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_default_params(*args, **kwargs)
      :classmethod:

      :abstractmethod:



   .. py:method:: get_log_prob_fn(x_o, use_ema=True, prior=None, model_extras=None, **kwargs)

      
      Get a log-probability function.

      :param x_o: Conditioning variable (observed data).
      :type x_o: array-like
      :param use_ema: Whether to use the EMA model. Default is True.
      :type use_ema: bool, optional
      :param prior: Obs-space prior for log-probability evaluation. The method's
                    prior lives on the full joint space ``(dim_joint, ch)`` and
                    cannot be automatically marginalized for arbitrary priors.

                    - **Default Gaussian**: auto-constructed — no need to provide.
                    - **Custom prior**: must supply the correct obs-space marginal.
      :type prior: numpyro.distributions.Distribution, optional
      :param model_extras: Additional model extras. Cannot override protected keys.
      :type model_extras: dict, optional
      :param \*\*kwargs: Forwarded to ``method.build_log_prob_fn``.

      :returns: ``log_prob_fn(x_1) -> log_prob``
      :rtype: Callable

      :raises ValueError: If the joint prior is non-Gaussian and no ``prior`` is provided.















      ..
          !! processed by numpydoc !!


   .. py:method:: get_loss_fn()

      
      Return the loss function for training/validation.
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_sampler(x_o, use_ema=True, model_extras=None, **sampler_kwargs)

      
      Get a sampler function.

      :param x_o: Conditioning variable (observed data).
      :type x_o: array-like
      :param use_ema: Whether to use the EMA model. Default is True.
      :type use_ema: bool, optional
      :param model_extras: Additional keyword arguments passed to the model during sampling
                           (e.g. ``{"edge_mask": mask}``). Cannot override the protected keys
                           ``cond``, ``obs_ids``, ``cond_ids``.
      :type model_extras: dict, optional
      :param \*\*sampler_kwargs: Forwarded to ``method.build_sampler_fn``.

      :returns: ``sampler(key, nsamples) -> samples``
      :rtype: Callable















      ..
          !! processed by numpydoc !!


   .. py:method:: init_pipeline_from_config(*args, **kwargs)
      :classmethod:

      :abstractmethod:


      
      Initialize the pipeline from a configuration file.

      :param train_dataset: Training dataset.
      :type train_dataset: iterable
      :param val_dataset: Validation dataset.
      :type val_dataset: iterable
      :param dim_obs: Dimensionality of the parameter (theta) space.
      :type dim_obs: int
      :param dim_cond: Dimensionality of the observation (x) space.
      :type dim_cond: int
      :param config_path: Path to the configuration file.
      :type config_path: str
      :param checkpoint_dir: Directory for saving checkpoints.
      :type checkpoint_dir: str

      :returns: **pipeline** -- An instance of the pipeline initialized from the configuration.
      :rtype: AbstractPipeline















      ..
          !! processed by numpydoc !!


   .. py:method:: log_prob(x_1, x_o, use_ema=True, prior=None, *, key=None, **kwargs)

      
      Compute log-probability of x_1 given x_o.

      :param x_1: Data samples to evaluate.
      :type x_1: array-like
      :param x_o: Conditioning variable.
      :type x_o: array-like
      :param use_ema: Use the EMA model. Default is True.
      :type use_ema: bool, optional
      :param prior: Obs-space prior distribution.
                    See :meth:`get_log_prob_fn` for details.
      :type prior: numpyro.distributions.Distribution, optional
      :param key: Required when ``exact_divergence=False`` (Hutchinson).
      :type key: jax.random.PRNGKey, optional
      :param \*\*kwargs: Forwarded to :meth:`get_log_prob_fn`.

      :returns: Log-probabilities.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:method:: sample(key, x_o, nsamples=10000, use_ema=True, **sampler_kwargs)

      
      Draw samples from the model.

      :param key: Random key.
      :type key: jax.random.PRNGKey
      :param x_o: Conditioning variable.
      :type x_o: array-like
      :param nsamples: Number of samples. Default is 10 000.
      :type nsamples: int, optional
      :param use_ema: Use the EMA model. Default is True.
      :type use_ema: bool, optional
      :param \*\*sampler_kwargs: Forwarded to :meth:`get_sampler`.

      :returns: Samples of shape ``(nsamples, dim_obs, ch_obs)``.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:attribute:: condition_mask_kind
      :value: 'structured'



   .. py:attribute:: dim_joint


   .. py:attribute:: loss_obj


   .. py:attribute:: method


   .. py:attribute:: path


.. py:class:: SimformerDiffusionPipeline(train_dataset, val_dataset, dim_obs, dim_cond, ch_obs = 1, params=None, training_config=None, edge_mask=None, condition_mask_kind='structured')

   Bases: :py:obj:`gensbi.recipes.joint_pipeline.JointPipeline`


   
   Model-agnostic joint pipeline parameterized by a ``GenerativeMethod``.

   Unlike the old method-specific pipeline classes, this class works with
   **any** generative method and **any** user-provided model that conforms
   to the ``JointWrapper`` interface.

   :param model: The model to be trained.
   :type model: nnx.Module
   :param train_dataset: Training dataset yielding concatenated ``x_1`` batches
                         (obs and cond concatenated along the token dimension).
   :type train_dataset: iterable
   :param val_dataset: Validation dataset.
   :type val_dataset: iterable
   :param dim_obs: Dimension of the observation/parameter space.
   :type dim_obs: int
   :param dim_cond: Dimension of the conditioning space.
   :type dim_cond: int
   :param method: Strategy object (e.g. ``FlowMatchingMethod()``,
                  ``DiffusionEDMMethod()``, ``ScoreMatchingMethod()``).
   :type method: GenerativeMethod
   :param ch_obs: Number of channels per token. Default is 1.
   :type ch_obs: int, optional
   :param condition_mask_kind: Kind of condition mask. One of ``"structured"`` or ``"posterior"``.
                               Default is ``"structured"``.
   :type condition_mask_kind: str, optional
   :param params: Model parameters (stored but not used directly).
   :type params: optional
   :param training_config: Training configuration.
   :type training_config: dict, optional

   .. rubric:: Examples

   >>> from gensbi.core import FlowMatchingMethod
   >>> pipeline = JointPipeline(
   ...     model=my_model,
   ...     train_dataset=train_ds,
   ...     val_dataset=val_ds,
   ...     dim_obs=2, dim_cond=7,
   ...     method=FlowMatchingMethod(),
   ... )















   ..
       !! processed by numpydoc !!

   .. py:method:: _make_model(params)

      
      Create and return the Simformer model to be trained.
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_default_params(dim_joint, in_channels)
      :classmethod:


      
      Return a dictionary of default model parameters.
















      ..
          !! processed by numpydoc !!


   .. py:method:: init_pipeline_from_config(train_dataset, val_dataset, dim_obs, dim_cond, config_path, checkpoint_dir, **kwargs)
      :classmethod:


      
      Initialize the pipeline from a configuration file.

      :param config_path: Path to the configuration file.
      :type config_path: str
      :param \*\*kwargs: Additional keyword arguments forwarded to the constructor.















      ..
          !! processed by numpydoc !!


   .. py:method:: sample(key, x_o, nsamples=10000, nsteps=18, use_ema=True, return_intermediates=False)

      
      Draw samples from the model.

      :param key: Random key.
      :type key: jax.random.PRNGKey
      :param x_o: Conditioning variable.
      :type x_o: array-like
      :param nsamples: Number of samples. Default is 10 000.
      :type nsamples: int, optional
      :param use_ema: Use the EMA model. Default is True.
      :type use_ema: bool, optional
      :param \*\*sampler_kwargs: Forwarded to :meth:`get_sampler`.

      :returns: Samples of shape ``(nsamples, dim_obs, ch_obs)``.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:attribute:: ch_obs
      :value: 1



   .. py:attribute:: dim_joint


   .. py:attribute:: edge_mask
      :value: None



   .. py:attribute:: ema_model


.. py:class:: SimformerFlowPipeline(train_dataset, val_dataset, dim_obs, dim_cond, ch_obs = 1, params=None, training_config=None, edge_mask=None, condition_mask_kind='structured')

   Bases: :py:obj:`gensbi.recipes.joint_pipeline.JointPipeline`


   
   Model-agnostic joint pipeline parameterized by a ``GenerativeMethod``.

   Unlike the old method-specific pipeline classes, this class works with
   **any** generative method and **any** user-provided model that conforms
   to the ``JointWrapper`` interface.

   :param model: The model to be trained.
   :type model: nnx.Module
   :param train_dataset: Training dataset yielding concatenated ``x_1`` batches
                         (obs and cond concatenated along the token dimension).
   :type train_dataset: iterable
   :param val_dataset: Validation dataset.
   :type val_dataset: iterable
   :param dim_obs: Dimension of the observation/parameter space.
   :type dim_obs: int
   :param dim_cond: Dimension of the conditioning space.
   :type dim_cond: int
   :param method: Strategy object (e.g. ``FlowMatchingMethod()``,
                  ``DiffusionEDMMethod()``, ``ScoreMatchingMethod()``).
   :type method: GenerativeMethod
   :param ch_obs: Number of channels per token. Default is 1.
   :type ch_obs: int, optional
   :param condition_mask_kind: Kind of condition mask. One of ``"structured"`` or ``"posterior"``.
                               Default is ``"structured"``.
   :type condition_mask_kind: str, optional
   :param params: Model parameters (stored but not used directly).
   :type params: optional
   :param training_config: Training configuration.
   :type training_config: dict, optional

   .. rubric:: Examples

   >>> from gensbi.core import FlowMatchingMethod
   >>> pipeline = JointPipeline(
   ...     model=my_model,
   ...     train_dataset=train_ds,
   ...     val_dataset=val_ds,
   ...     dim_obs=2, dim_cond=7,
   ...     method=FlowMatchingMethod(),
   ... )















   ..
       !! processed by numpydoc !!

   .. py:method:: _make_model(params)

      
      Create and return the Simformer model to be trained.
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_default_params(dim_joint, in_channels)
      :classmethod:


      
      Return a dictionary of default model parameters.
















      ..
          !! processed by numpydoc !!


   .. py:method:: init_pipeline_from_config(train_dataset, val_dataset, dim_obs, dim_cond, config_path, checkpoint_dir, **kwargs)
      :classmethod:


      
      Initialize the pipeline from a configuration file.

      :param config_path: Path to the configuration file.
      :type config_path: str
      :param \*\*kwargs: Additional keyword arguments forwarded to the constructor.















      ..
          !! processed by numpydoc !!


   .. py:method:: sample(key, x_o, nsamples=10000, step_size=0.01, use_ema=True, time_grid=None)

      
      Draw samples from the model.

      :param key: Random key.
      :type key: jax.random.PRNGKey
      :param x_o: Conditioning variable.
      :type x_o: array-like
      :param nsamples: Number of samples. Default is 10 000.
      :type nsamples: int, optional
      :param use_ema: Use the EMA model. Default is True.
      :type use_ema: bool, optional
      :param \*\*sampler_kwargs: Forwarded to :meth:`get_sampler`.

      :returns: Samples of shape ``(nsamples, dim_obs, ch_obs)``.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:attribute:: ch_obs
      :value: 1



   .. py:attribute:: dim_joint


   .. py:attribute:: edge_mask
      :value: None



   .. py:attribute:: ema_model


.. py:class:: SimformerSMPipeline(train_dataset, val_dataset, dim_obs, dim_cond, ch_obs = 1, sde_type = 'VP', params=None, training_config=None, edge_mask=None, condition_mask_kind='structured')

   Bases: :py:obj:`gensbi.recipes.joint_pipeline.JointPipeline`


   
   Model-agnostic joint pipeline parameterized by a ``GenerativeMethod``.

   Unlike the old method-specific pipeline classes, this class works with
   **any** generative method and **any** user-provided model that conforms
   to the ``JointWrapper`` interface.

   :param model: The model to be trained.
   :type model: nnx.Module
   :param train_dataset: Training dataset yielding concatenated ``x_1`` batches
                         (obs and cond concatenated along the token dimension).
   :type train_dataset: iterable
   :param val_dataset: Validation dataset.
   :type val_dataset: iterable
   :param dim_obs: Dimension of the observation/parameter space.
   :type dim_obs: int
   :param dim_cond: Dimension of the conditioning space.
   :type dim_cond: int
   :param method: Strategy object (e.g. ``FlowMatchingMethod()``,
                  ``DiffusionEDMMethod()``, ``ScoreMatchingMethod()``).
   :type method: GenerativeMethod
   :param ch_obs: Number of channels per token. Default is 1.
   :type ch_obs: int, optional
   :param condition_mask_kind: Kind of condition mask. One of ``"structured"`` or ``"posterior"``.
                               Default is ``"structured"``.
   :type condition_mask_kind: str, optional
   :param params: Model parameters (stored but not used directly).
   :type params: optional
   :param training_config: Training configuration.
   :type training_config: dict, optional

   .. rubric:: Examples

   >>> from gensbi.core import FlowMatchingMethod
   >>> pipeline = JointPipeline(
   ...     model=my_model,
   ...     train_dataset=train_ds,
   ...     val_dataset=val_ds,
   ...     dim_obs=2, dim_cond=7,
   ...     method=FlowMatchingMethod(),
   ... )















   ..
       !! processed by numpydoc !!

   .. py:method:: _make_model(params)

      
      Create and return the Simformer model to be trained.
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_default_params(dim_joint, in_channels)
      :classmethod:


      
      Return a dictionary of default model parameters.
















      ..
          !! processed by numpydoc !!


   .. py:method:: init_pipeline_from_config(train_dataset, val_dataset, dim_obs, dim_cond, config_path, checkpoint_dir, **kwargs)
      :classmethod:


      
      Initialize the pipeline from a configuration file.

      :param config_path: Path to the configuration file.
      :type config_path: str
      :param \*\*kwargs: Additional keyword arguments forwarded to the constructor
                         (e.g. ``sde_type="VE"`` for score matching).















      ..
          !! processed by numpydoc !!


   .. py:method:: sample(key, x_o, nsamples=10000, nsteps=1000, use_ema=True, return_intermediates=False)

      
      Draw samples from the model.

      :param key: Random key.
      :type key: jax.random.PRNGKey
      :param x_o: Conditioning variable.
      :type x_o: array-like
      :param nsamples: Number of samples. Default is 10 000.
      :type nsamples: int, optional
      :param use_ema: Use the EMA model. Default is True.
      :type use_ema: bool, optional
      :param \*\*sampler_kwargs: Forwarded to :meth:`get_sampler`.

      :returns: Samples of shape ``(nsamples, dim_obs, ch_obs)``.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:attribute:: ch_obs
      :value: 1



   .. py:attribute:: dim_joint


   .. py:attribute:: edge_mask
      :value: None



   .. py:attribute:: ema_model


.. py:class:: UnconditionalPipeline(model, train_dataset, val_dataset, dim_obs, method, ch_obs=1, params=None, training_config=None)

   Bases: :py:obj:`gensbi.recipes.pipeline.AbstractPipeline`


   
   Model-agnostic unconditional pipeline parameterized by a ``GenerativeMethod``.

   Unlike the old method-specific pipeline classes, this
   class works with **any** generative method and **any** user-provided
   model that conforms to the ``UnconditionalWrapper`` interface.

   :param model: The model to be trained.
   :type model: nnx.Module
   :param train_dataset: Training dataset yielding ``x_1`` batches (not tuples).
   :type train_dataset: iterable
   :param val_dataset: Validation dataset.
   :type val_dataset: iterable
   :param dim_obs: Dimension of the data space.
   :type dim_obs: int
   :param method: Strategy object (e.g. ``FlowMatchingMethod()``,
                  ``DiffusionEDMMethod()``, ``ScoreMatchingMethod()``).
   :type method: GenerativeMethod
   :param ch_obs: Number of channels per token. Default is 1.
   :type ch_obs: int, optional
   :param params: Model parameters (stored but not used directly).
   :type params: optional
   :param training_config: Training configuration.
   :type training_config: dict, optional

   .. rubric:: Examples

   >>> from gensbi.core import FlowMatchingMethod
   >>> pipeline = UnconditionalPipeline(
   ...     model=my_model,
   ...     train_dataset=train_ds,
   ...     val_dataset=val_ds,
   ...     dim_obs=9,
   ...     method=FlowMatchingMethod(),
   ... )















   ..
       !! processed by numpydoc !!

   .. py:method:: _make_model()
      :abstractmethod:


      
      Create and return the model to be trained.
















      ..
          !! processed by numpydoc !!


   .. py:method:: _wrap_model()

      
      Wrap the model for evaluation (either using JointWrapper or ConditionalWrapper).
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_default_params(*args, **kwargs)
      :classmethod:

      :abstractmethod:



   .. py:method:: get_log_prob_fn(use_ema=True, **kwargs)

      
      Get a log-probability function.

      :param use_ema: Whether to use the EMA model. Default is True.
      :type use_ema: bool, optional
      :param \*\*kwargs: Forwarded to ``method.build_log_prob_fn``.

      :returns: ``log_prob_fn(x_1) -> log_prob``
      :rtype: Callable















      ..
          !! processed by numpydoc !!


   .. py:method:: get_loss_fn()

      
      Return the loss function for training/validation.
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_sampler(use_ema=True, **sampler_kwargs)

      
      Get a sampler function.

      :param use_ema: Whether to use the EMA model. Default is True.
      :type use_ema: bool, optional
      :param \*\*sampler_kwargs: Forwarded to ``method.build_sampler_fn``.

      :returns: ``sampler(key, nsamples) -> samples``
      :rtype: Callable















      ..
          !! processed by numpydoc !!


   .. py:method:: init_pipeline_from_config(*args, **kwargs)
      :classmethod:

      :abstractmethod:


      
      Initialize the pipeline from a configuration file.

      :param train_dataset: Training dataset.
      :type train_dataset: iterable
      :param val_dataset: Validation dataset.
      :type val_dataset: iterable
      :param dim_obs: Dimensionality of the parameter (theta) space.
      :type dim_obs: int
      :param dim_cond: Dimensionality of the observation (x) space.
      :type dim_cond: int
      :param config_path: Path to the configuration file.
      :type config_path: str
      :param checkpoint_dir: Directory for saving checkpoints.
      :type checkpoint_dir: str

      :returns: **pipeline** -- An instance of the pipeline initialized from the configuration.
      :rtype: AbstractPipeline















      ..
          !! processed by numpydoc !!


   .. py:method:: log_prob(x_1, use_ema=True, *, key=None, **kwargs)

      
      Compute log-probability of x_1.

      :param x_1: Data samples to evaluate.
      :type x_1: array-like
      :param use_ema: Use the EMA model. Default is True.
      :type use_ema: bool, optional
      :param key: Required when ``exact_divergence=False`` (Hutchinson).
      :type key: jax.random.PRNGKey, optional
      :param \*\*kwargs: Forwarded to :meth:`get_log_prob_fn`.

      :returns: Log-probabilities.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:method:: sample(key, nsamples=10000, use_ema=True, **sampler_kwargs)

      
      Draw samples from the model.

      :param key: Random key.
      :type key: jax.random.PRNGKey
      :param nsamples: Number of samples. Default is 10 000.
      :type nsamples: int, optional
      :param use_ema: Use the EMA model. Default is True.
      :type use_ema: bool, optional
      :param \*\*sampler_kwargs: Forwarded to :meth:`get_sampler`.

      :returns: Samples of shape ``(nsamples, dim_obs, ch_obs)``.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:method:: sample_batched(*args, **kwargs)
      :abstractmethod:


      
      Generate samples from the trained model in batches.

      :param key: Random number generator key.
      :type key: jax.random.PRNGKey
      :param x_o: Conditioning variable (e.g., observed data).
      :type x_o: array-like
      :param nsamples: Number of samples to generate.
      :type nsamples: int
      :param chunk_size: Size of each batch for sampling. Default is 50.
      :type chunk_size: int, optional
      :param show_progress_bars: Whether to display progress bars during sampling. Default is True.
      :type show_progress_bars: bool, optional
      :param args: Additional positional arguments for the sampler.
      :type args: tuple
      :param kwargs: Additional keyword arguments for the sampler.
      :type kwargs: dict

      :returns: **samples** -- Generated samples of shape (nsamples, batch_size_cond, dim_obs, ch_obs).
      :rtype: array-like















      ..
          !! processed by numpydoc !!


   .. py:attribute:: loss_obj


   .. py:attribute:: method


   .. py:attribute:: path


