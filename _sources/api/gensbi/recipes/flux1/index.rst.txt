gensbi.recipes.flux1
====================

.. py:module:: gensbi.recipes.flux1

.. autoapi-nested-parse::

   Pipeline for training and using a Flux1 model for simulation-based inference.

   ..
       !! processed by numpydoc !!


Classes
-------

.. autoapisummary::

   gensbi.recipes.flux1.Flux1DiffusionPipeline
   gensbi.recipes.flux1.Flux1FlowPipeline
   gensbi.recipes.flux1.Flux1SMPipeline


Functions
---------

.. autoapisummary::

   gensbi.recipes.flux1._flux1_config_from_path
   gensbi.recipes.flux1.get_default_flux1_params
   gensbi.recipes.flux1.parse_flux1_params
   gensbi.recipes.flux1.parse_training_config


Module Contents
---------------

.. py:class:: Flux1DiffusionPipeline(train_dataset, val_dataset, dim_obs, dim_cond, ch_obs=1, ch_cond=1, params=None, training_config=None)

   Bases: :py:obj:`gensbi.recipes.conditional_pipeline.ConditionalDiffusionPipeline`


   
   Diffusion pipeline for training and using a Conditional model for simulation-based inference.

   :param train_dataset: Training dataset.
   :type train_dataset: grain dataset or iterator over batches
   :param val_dataset: Validation dataset.
   :type val_dataset: grain dataset or iterator over batches
   :param dim_obs: Dimension of the parameter space (number of tokens).
                   Can represent unstructured data, time-series, or patchified 2D images. For images, provide a tuple (height, width).
   :type dim_obs: int or tuple of int
   :param dim_cond: Dimension of the observation space (number of tokens).
                    Can represent unstructured data, time-series, or patchified 2D images. For images, provide a tuple (height, width).
   :type dim_cond: int or tuple of int
   :param params: Parameters for the Conditional model. If None, default parameters are used.
   :type params: ConditionalParams, optional
   :param training_config: Configuration for training. If None, default configuration is used.
   :type training_config: dict, optional

   .. rubric:: Examples

   Minimal example on how to instantiate and use the ConditionalDiffusionPipeline:

   .. literalinclude:: /examples/conditional_diffusion_pipeline.py
       :language: python
       :linenos:

   .. image:: /examples/conditional_diffusion_pipeline_marginals.png
       :width: 600

   .. note::
       If you plan on using multiprocessing prefetching, ensure that your script is wrapped
       in a ``if __name__ == "__main__":`` guard.
       See https://docs.python.org/3/library/multiprocessing.html

   .. note::
       Sampling in the latent space (latent diffusion/flow) is not currently supported.















   ..
       !! processed by numpydoc !!

   .. py:method:: _make_model(params)

      
      Create and return the Flux1 model to be trained.
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_default_params(dim_obs, dim_cond, ch_obs, ch_cond)
      :classmethod:


      
      Return a dictionary of default model parameters.
















      ..
          !! processed by numpydoc !!


   .. py:method:: init_pipeline_from_config(train_dataset, val_dataset, dim_obs, dim_cond, config_path, checkpoint_dir)
      :classmethod:


      
      Initialize the pipeline from a configuration file.

      :param config_path: Path to the configuration file.
      :type config_path: str















      ..
          !! processed by numpydoc !!


   .. py:attribute:: ch_cond
      :value: 1



   .. py:attribute:: ch_obs
      :value: 1



   .. py:attribute:: dim_cond


   .. py:attribute:: dim_obs


   .. py:attribute:: ema_model


.. py:class:: Flux1FlowPipeline(train_dataset, val_dataset, dim_obs, dim_cond, ch_obs=1, ch_cond=1, params=None, training_config=None)

   Bases: :py:obj:`gensbi.recipes.conditional_pipeline.ConditionalFlowPipeline`


   
   Flow pipeline for training and using a Conditional model for simulation-based inference.

   :param model: The model to be trained.
   :type model: nnx.Module
   :param train_dataset: Training dataset.
   :type train_dataset: grain dataset or iterator over batches
   :param val_dataset: Validation dataset.
   :type val_dataset: grain dataset or iterator over batches
   :param dim_obs: Dimension of the parameter space (number of tokens).
                   Can represent unstructured data, time-series, or patchified 2D images. For images, provide a tuple (height, width).
   :type dim_obs: int or tuple of int
   :param dim_cond: Dimension of the observation space (number of tokens).
                    Can represent unstructured data, time-series, or patchified 2D images. For images, provide a tuple (height, width).
   :type dim_cond: int or tuple of int
   :param ch_obs: Number of channels per token in the observation data. Default is 1.
   :type ch_obs: int, optional
   :param ch_cond: Number of channels per token in the conditional data. Default is 1.
   :type ch_cond: int, optional
   :param params: Parameters for the Conditional model. If None, default parameters are used.
   :type params: ConditionalParams, optional
   :param training_config: Configuration for training. If None, default configuration is used.
   :type training_config: dict, optional

   .. rubric:: Examples

   Minimal example on how to instantiate and use the ConditionalFlowPipeline:

   .. literalinclude:: /examples/conditional_flow_pipeline.py
       :language: python
       :linenos:

   .. image:: /examples/conditional_flow_pipeline_marginals.png
       :width: 600

   .. note::
       If you plan on using multiprocessing prefetching, ensure that your script is wrapped
       in a ``if __name__ == "__main__":`` guard.
       See https://docs.python.org/3/library/multiprocessing.html

   .. note::
       Sampling in the latent space (latent diffusion/flow) is not currently supported.















   ..
       !! processed by numpydoc !!

   .. py:method:: _make_model(params)

      
      Create and return the Flux1 model to be trained.
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_default_params(dim_obs, dim_cond, ch_obs, ch_cond)
      :classmethod:


      
      Return a dictionary of default model parameters.
















      ..
          !! processed by numpydoc !!


   .. py:method:: init_pipeline_from_config(train_dataset, val_dataset, dim_obs, dim_cond, config_path, checkpoint_dir)
      :classmethod:


      
      Initialize the pipeline from a configuration file.

      :param config_path: Path to the configuration file.
      :type config_path: str















      ..
          !! processed by numpydoc !!


   .. py:attribute:: ch_cond
      :value: 1



   .. py:attribute:: ch_obs
      :value: 1



   .. py:attribute:: dim_cond


   .. py:attribute:: dim_obs


   .. py:attribute:: ema_model


.. py:class:: Flux1SMPipeline(train_dataset, val_dataset, dim_obs, dim_cond, ch_obs=1, ch_cond=1, sde_type = 'VP', params=None, training_config=None)

   Bases: :py:obj:`gensbi.recipes.conditional_pipeline.ConditionalSMPipeline`


   
   Score matching pipeline for training and using a Conditional model for simulation-based inference.

   Supports both Variance Preserving (VP) and Variance Exploding (VE) SDE formulations.

   :param model: The model to be trained.
   :type model: nnx.Module
   :param train_dataset: Training dataset.
   :type train_dataset: grain dataset or iterator over batches
   :param val_dataset: Validation dataset.
   :type val_dataset: grain dataset or iterator over batches
   :param dim_obs: Dimension of the parameter space (number of tokens).
   :type dim_obs: int or tuple of int
   :param dim_cond: Dimension of the observation space (number of tokens).
   :type dim_cond: int or tuple of int
   :param ch_obs: Number of channels per token in the observation data. Default is 1.
   :type ch_obs: int, optional
   :param ch_cond: Number of channels per token in the conditional data. Default is 1.
   :type ch_cond: int, optional
   :param sde_type: Type of SDE to use. One of ``"VP"`` (Variance Preserving) or ``"VE"`` (Variance Exploding).
   :type sde_type: str
   :param id_embedding_strategy: Embedding strategy for observation and conditioning IDs.
   :type id_embedding_strategy: tuple of str, optional
   :param params: Parameters for the model.
   :type params: optional
   :param training_config: Configuration for training. If None, default configuration is used.
   :type training_config: dict, optional















   ..
       !! processed by numpydoc !!

   .. py:method:: _make_model(params)

      
      Create and return the Flux1 model to be trained.
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_default_params(dim_obs, dim_cond, ch_obs, ch_cond)
      :classmethod:


      
      Return a dictionary of default model parameters.
















      ..
          !! processed by numpydoc !!


   .. py:method:: init_pipeline_from_config(train_dataset, val_dataset, dim_obs, dim_cond, config_path, checkpoint_dir)
      :classmethod:


      
      Initialize the pipeline from a configuration file.

      :param config_path: Path to the configuration file.
      :type config_path: str















      ..
          !! processed by numpydoc !!


   .. py:attribute:: ch_cond
      :value: 1



   .. py:attribute:: ch_obs
      :value: 1



   .. py:attribute:: dim_cond


   .. py:attribute:: dim_obs


   .. py:attribute:: ema_model


.. py:function:: _flux1_config_from_path(config_path, dim_obs, dim_cond)

   
   Helper to parse common configuration for Flux1 pipelines.

   :returns: * **params** (*Flux1Params*) -- The parsed model parameters.
             * **training_config** (*dict*) -- The parsed training configuration.
             * **method** (*str*) -- The methodology (flow or diffusion) specified in the config.















   ..
       !! processed by numpydoc !!

.. py:function:: get_default_flux1_params(dim_obs, dim_cond, ch_obs = 1, ch_cond = 1)

   
   Return default parameters for the Flux1 model.
















   ..
       !! processed by numpydoc !!

.. py:function:: parse_flux1_params(config_path)

   
   Parse a Flux1 configuration file.

   :param config_path: Path to the configuration file.
   :type config_path: str

   :returns: **config** -- Parsed configuration dictionary.
   :rtype: dict















   ..
       !! processed by numpydoc !!

.. py:function:: parse_training_config(config_path)

   
   Parse a training configuration file.

   :param config_path: Path to the configuration file.
   :type config_path: str

   :returns: **config** -- Parsed configuration dictionary.
   :rtype: dict















   ..
       !! processed by numpydoc !!

