gensbi.recipes.simformer
========================

.. py:module:: gensbi.recipes.simformer

.. autoapi-nested-parse::

   Pipeline for training and using a Simformer model for simulation-based inference.

   ..
       !! processed by numpydoc !!


Classes
-------

.. autoapisummary::

   gensbi.recipes.simformer.SimformerDiffusionPipeline
   gensbi.recipes.simformer.SimformerFlowPipeline
   gensbi.recipes.simformer.SimformerSMPipeline


Functions
---------

.. autoapisummary::

   gensbi.recipes.simformer._simformer_config_from_path
   gensbi.recipes.simformer.get_default_simformer_params
   gensbi.recipes.simformer.parse_simformer_params
   gensbi.recipes.simformer.parse_training_config


Module Contents
---------------

.. py:class:: SimformerDiffusionPipeline(train_dataset, val_dataset, dim_obs, dim_cond, ch_obs = 1, params=None, training_config=None, edge_mask=None, condition_mask_kind='structured')

   Bases: :py:obj:`gensbi.recipes.joint_pipeline.JointDiffusionPipeline`


   
   Diffusion pipeline for training and using a Joint model for simulation-based inference.

   :param train_dataset: Training dataset.
   :type train_dataset: grain dataset or iterator over batches
   :param val_dataset: Validation dataset.
   :type val_dataset: grain dataset or iterator over batches
   :param dim_obs: Dimension of the parameter space.
   :type dim_obs: int
   :param dim_cond: Dimension of the observation space.
   :type dim_cond: int
   :param ch_obs: Number of channels for the observation space. Default is 1.
   :type ch_obs: int, optional
   :param params: Parameters for the Joint model. If None, default parameters are used.
   :type params: optional
   :param training_config: Configuration for training. If None, default configuration is used.
   :type training_config: dict, optional
   :param condition_mask_kind: Kind of condition mask to use. One of ["structured", "posterior"].
   :type condition_mask_kind: str, optional

   .. rubric:: Examples

   Minimal example on how to instantiate and use the JointDiffusionPipeline:

   .. literalinclude:: /examples/joint_diffusion_pipeline.py
       :language: python
       :linenos:

   .. image:: /examples/joint_diffusion_pipeline_marginals.png
       :width: 600

   .. note::
       If you plan on using multiprocessing prefetching, ensure that your script is wrapped
       in a ``if __name__ == "__main__":`` guard.
       See https://docs.python.org/3/library/multiprocessing.html















   ..
       !! processed by numpydoc !!

   .. py:method:: _make_model(params)

      
      Create and return the Simformer model to be trained.
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_default_params(dim_joint, in_channels)
      :classmethod:


      
      Return a dictionary of default model parameters.
















      ..
          !! processed by numpydoc !!


   .. py:method:: init_pipeline_from_config(train_dataset, val_dataset, dim_obs, dim_cond, config_path, checkpoint_dir)
      :classmethod:


      
      Initialize the pipeline from a configuration file.

      :param config_path: Path to the configuration file.
      :type config_path: str















      ..
          !! processed by numpydoc !!


   .. py:method:: sample(key, x_o, nsamples=10000, nsteps=18, use_ema=True, return_intermediates=False)

      
      Draw samples from the diffusion model.

      Convenience method that internally calls :meth:`get_sampler` and
      immediately evaluates the returned sampler function.

      :param key: JAX random key used for sampling.
      :type key: jax.random.PRNGKey
      :param x_o: Conditioning variable (e.g., observed data).
      :type x_o: array-like
      :param nsamples: Number of samples to draw. Default is 10 000.
      :type nsamples: int, optional
      :param nsteps: Number of sampling steps. Default is 18.
      :type nsteps: int, optional
      :param use_ema: Whether to use the EMA model. Default is True.
      :type use_ema: bool, optional
      :param return_intermediates: Whether to return intermediate steps. Default is False.
      :type return_intermediates: bool, optional
      :param solver: A tuple ``(SolverClass, solver_kwargs)`` specifying the solver
                     class and its keyword arguments. Defaults to ``(EDMSolver, {})``.

                     EDM-specific options can be provided in the kwargs dict:

                     - ``solver_scheduler``: override the path's scheduler for
                       sampling (also used for ``sample_prior``).
                     - ``solver_params``: dict of EDM solver parameters
                       (``S_churn``, ``S_min``, ``S_max``, ``S_noise``).
      :type solver: tuple of (type, dict), optional
      :param \*\*model_extras: Additional keyword arguments passed to the model.
      :type \*\*model_extras: dict

      :returns: Sampled output of shape ``(nsamples, dim_obs, ch_obs)``.
      :rtype: Array

      .. rubric:: Examples

      Using the default EDM solver:

      >>> samples = pipeline.sample(key, x_o, nsamples=1000)

      Using a custom scheduler:

      >>> from gensbi.diffusion.solver import EDMSolver
      >>> from gensbi.diffusion.path.scheduler import VEEdmScheduler
      >>> solver = (EDMSolver, {"solver_scheduler": VEEdmScheduler()})
      >>> samples = pipeline.sample(key, x_o, solver=solver)















      ..
          !! processed by numpydoc !!


   .. py:attribute:: ch_obs
      :value: 1



   .. py:attribute:: dim_joint


   .. py:attribute:: edge_mask
      :value: None



   .. py:attribute:: ema_model


.. py:class:: SimformerFlowPipeline(train_dataset, val_dataset, dim_obs, dim_cond, ch_obs = 1, params=None, training_config=None, edge_mask=None, condition_mask_kind='structured')

   Bases: :py:obj:`gensbi.recipes.joint_pipeline.JointFlowPipeline`


   
   Flow pipeline for training and using a Joint model for simulation-based inference.

   :param train_dataset: Training dataset.
   :type train_dataset: grain dataset or iterator over batches
   :param val_dataset: Validation dataset.
   :type val_dataset: grain dataset or iterator over batches
   :param dim_obs: Dimension of the parameter space.
   :type dim_obs: int
   :param dim_cond: Dimension of the observation space.
   :type dim_cond: int
   :param ch_obs: Number of channels for the observation space. Default is 1.
   :type ch_obs: int, optional
   :param params: Parameters for the Joint model. If None, default parameters are used.
   :type params: JointParams, optional
   :param training_config: Configuration for training. If None, default configuration is used.
   :type training_config: dict, optional
   :param condition_mask_kind: Kind of condition mask to use. One of ["structured", "posterior"].
   :type condition_mask_kind: str, optional

   .. rubric:: Examples

   Minimal example on how to instantiate and use the JointFlowPipeline:

   .. literalinclude:: /examples/joint_flow_pipeline.py
       :language: python
       :linenos:

   .. image:: /examples/joint_flow_pipeline_marginals.png
       :width: 600

   .. note::
       If you plan on using multiprocessing prefetching, ensure that your script is wrapped
       in a ``if __name__ == "__main__":`` guard.
       See https://docs.python.org/3/library/multiprocessing.html















   ..
       !! processed by numpydoc !!

   .. py:method:: _make_model(params)

      
      Create and return the Simformer model to be trained.
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_default_params(dim_joint, in_channels)
      :classmethod:


      
      Return a dictionary of default model parameters.
















      ..
          !! processed by numpydoc !!


   .. py:method:: init_pipeline_from_config(train_dataset, val_dataset, dim_obs, dim_cond, config_path, checkpoint_dir)
      :classmethod:


      
      Initialize the pipeline from a configuration file.

      :param config_path: Path to the configuration file.
      :type config_path: str















      ..
          !! processed by numpydoc !!


   .. py:method:: sample(key, x_o, nsamples=10000, step_size=0.01, use_ema=True, time_grid=None)

      
      Draw samples from the flow model.

      Convenience method that internally calls :meth:`get_sampler` and
      immediately evaluates the returned sampler function.

      :param key: JAX random key used for sampling.
      :type key: jax.random.PRNGKey
      :param x_o: Conditioning variable (e.g., observed data).
      :type x_o: array-like
      :param nsamples: Number of samples to draw. Default is 10 000.
      :type nsamples: int, optional
      :param step_size: Step size for the solver. Default is 0.01.
      :type step_size: float, optional
      :param use_ema: Whether to use the EMA model. Default is True.
      :type use_ema: bool, optional
      :param time_grid: Time grid for intermediate steps. If None, uses
                        ``jnp.linspace(0, 1, int(1 / step_size) + 1)``.
      :type time_grid: Array, optional
      :param solver: A tuple ``(SolverClass, solver_kwargs)`` specifying the solver
                     class and its keyword arguments. Defaults to ``(ODESolver, {})``.

                     To use an SDE solver instead:

                     >>> from gensbi.flow_matching.solver import ZeroEndsSolver
                     >>> solver = (ZeroEndsSolver, {"mu0": 0.0, "sigma0": 0.01})
                     >>> samples = pipeline.sample(key, x_o, solver=solver)
      :type solver: tuple of (type, dict), optional
      :param \*\*model_extras: Additional keyword arguments passed to the model.
      :type \*\*model_extras: dict

      :returns: Sampled output of shape ``(nsamples, dim_obs, ch_obs)``.
      :rtype: Array

      .. rubric:: Examples

      Using the default ODE solver:

      >>> samples = pipeline.sample(key, x_o, nsamples=1000)

      Using the ZeroEnds SDE solver:

      >>> from gensbi.flow_matching.solver import ZeroEndsSolver
      >>> solver = (ZeroEndsSolver, {"mu0": 0.0, "sigma0": 0.01})
      >>> samples = pipeline.sample(key, x_o, solver=solver)















      ..
          !! processed by numpydoc !!


   .. py:attribute:: ch_obs
      :value: 1



   .. py:attribute:: dim_joint


   .. py:attribute:: edge_mask
      :value: None



   .. py:attribute:: ema_model


.. py:class:: SimformerSMPipeline(train_dataset, val_dataset, dim_obs, dim_cond, ch_obs = 1, sde_type = 'VP', params=None, training_config=None, edge_mask=None, condition_mask_kind='structured')

   Bases: :py:obj:`gensbi.recipes.joint_pipeline.JointSMPipeline`


   
   Score matching pipeline for training and using a Joint model for simulation-based inference.

   Supports both Variance Preserving (VP) and Variance Exploding (VE) SDE formulations.

   :param model: The model to be trained.
   :type model: nnx.Module
   :param train_dataset: Training dataset.
   :type train_dataset: grain dataset or iterator over batches
   :param val_dataset: Validation dataset.
   :type val_dataset: grain dataset or iterator over batches
   :param dim_obs: Dimension of the parameter space.
   :type dim_obs: int
   :param dim_cond: Dimension of the observation space.
   :type dim_cond: int
   :param ch_obs: Number of channels for the observation space. Default is 1.
   :type ch_obs: int, optional
   :param sde_type: Type of SDE to use. One of ``"VP"`` (Variance Preserving) or ``"VE"`` (Variance Exploding).
   :type sde_type: str
   :param params: Parameters for the Joint model. If None, default parameters are used.
   :type params: optional
   :param training_config: Configuration for training. If None, default configuration is used.
   :type training_config: dict, optional
   :param condition_mask_kind: Kind of condition mask to use. One of ["structured", "posterior"].
   :type condition_mask_kind: str, optional















   ..
       !! processed by numpydoc !!

   .. py:method:: _make_model(params)

      
      Create and return the Simformer model to be trained.
















      ..
          !! processed by numpydoc !!


   .. py:method:: get_default_params(dim_joint, in_channels)
      :classmethod:


      
      Return a dictionary of default model parameters.
















      ..
          !! processed by numpydoc !!


   .. py:method:: init_pipeline_from_config(train_dataset, val_dataset, dim_obs, dim_cond, config_path, checkpoint_dir)
      :classmethod:


      
      Initialize the pipeline from a configuration file.

      :param config_path: Path to the configuration file.
      :type config_path: str















      ..
          !! processed by numpydoc !!


   .. py:method:: sample(key, x_o, nsamples=10000, nsteps=1000, use_ema=True, return_intermediates=False)

      
      Draw samples from the score matching model.

      Convenience method that internally calls :meth:`get_sampler` and
      immediately evaluates the returned sampler function.

      :param key: JAX random key used for sampling.
      :type key: jax.random.PRNGKey
      :param x_o: Conditioning variable (e.g., observed data).
      :type x_o: array-like
      :param nsamples: Number of samples to draw. Default is 10 000.
      :type nsamples: int, optional
      :param nsteps: Number of sampling steps. Default is 1000.
      :type nsteps: int, optional
      :param use_ema: Whether to use the EMA model. Default is True.
      :type use_ema: bool, optional
      :param return_intermediates: Whether to return intermediate steps. Default is False.
      :type return_intermediates: bool, optional
      :param solver: A tuple ``(SolverClass, solver_kwargs)`` specifying the solver
                     class and its keyword arguments. Defaults to ``(SMSolver, {})``.

                     To use the probability flow ODE solver instead:

                     >>> from gensbi.diffusion.solver import SMPFSolver
                     >>> solver = (SMPFSolver, {})
                     >>> samples = pipeline.sample(key, x_o, solver=solver)
      :type solver: tuple of (type, dict), optional
      :param \*\*model_extras: Additional keyword arguments passed to the model.
      :type \*\*model_extras: dict

      :returns: Sampled output of shape ``(nsamples, dim_obs, ch_obs)``.
      :rtype: Array

      .. rubric:: Examples

      Using the default SDE solver:

      >>> samples = pipeline.sample(key, x_o, nsamples=1000)

      Using the probability flow ODE solver:

      >>> from gensbi.diffusion.solver import SMPFSolver
      >>> solver = (SMPFSolver, {})
      >>> samples = pipeline.sample(key, x_o, solver=solver)















      ..
          !! processed by numpydoc !!


   .. py:attribute:: ch_obs
      :value: 1



   .. py:attribute:: dim_joint


   .. py:attribute:: edge_mask
      :value: None



   .. py:attribute:: ema_model


.. py:function:: _simformer_config_from_path(config_path, dim_joint)

   
   Helper to parse common configuration for Simformer pipelines.

   :returns: * **params** (*SimformerParams*) -- The parsed model parameters.
             * **training_config** (*dict*) -- The parsed training configuration.
             * **method** (*str*) -- The methodology (flow or diffusion) specified in the config.















   ..
       !! processed by numpydoc !!

.. py:function:: get_default_simformer_params(dim_joint, in_channels = 1)

   
   Return default parameters for the Simformer model.
















   ..
       !! processed by numpydoc !!

.. py:function:: parse_simformer_params(config_path)

   
   Parse a Simformer configuration file.

   :param config_path: Path to the configuration file.
   :type config_path: str

   :returns: **config** -- Parsed configuration dictionary.
   :rtype: dict















   ..
       !! processed by numpydoc !!

.. py:function:: parse_training_config(config_path)

   
   Parse a training configuration file.

   :param config_path: Path to the configuration file.
   :type config_path: str

   :returns: **config** -- Parsed configuration dictionary.
   :rtype: dict















   ..
       !! processed by numpydoc !!

