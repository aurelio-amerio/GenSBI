gensbi.recipes.utils
====================

.. py:module:: gensbi.recipes.utils


Attributes
----------

.. autoapisummary::

   gensbi.recipes.utils._EMBEDDINGS_1D
   gensbi.recipes.utils._EMBEDDINGS_2D
   gensbi.recipes.utils._MOVED_TO_PATCHING


Functions
---------

.. autoapisummary::

   gensbi.recipes.utils.__getattr__
   gensbi.recipes.utils._normalize_patch_size
   gensbi.recipes.utils._require_channel
   gensbi.recipes.utils._resolve_embedding_ids
   gensbi.recipes.utils._single_obs
   gensbi.recipes.utils.build_edm_path
   gensbi.recipes.utils.build_sm_path
   gensbi.recipes.utils.init_ids_1d
   gensbi.recipes.utils.init_ids_2d
   gensbi.recipes.utils.init_ids_joint
   gensbi.recipes.utils.parse_training_config
   gensbi.recipes.utils.scale_lr


Module Contents
---------------

.. py:function:: __getattr__(name)

.. py:function:: _normalize_patch_size(size)

   
   Normalize a patch-size spec into an ``(obs, cond)`` tuple.

   :param size: A single int is broadcast to both inputs (``8 -> (8, 8)``). A
                length-2 tuple is taken as ``(obs_size, cond_size)`` so the two
                inputs can use different patch sizes. Use ``1`` for an input that
                is not patchified.
   :type size: int or tuple of int

   :returns: ``(obs_size, cond_size)``.
   :rtype: tuple of int















   ..
       !! processed by numpydoc !!

.. py:function:: _require_channel(x, name='input')

   
   Enforce a tabular channel axis (B, dim, C); reject a bare (B, dim).
















   ..
       !! processed by numpydoc !!

.. py:function:: _resolve_embedding_ids(dim, strategy, semantic_id, size = 2)

   
   Resolve ID embeddings by strategy name.

   :param dim: Dimension specification (number of tokens, or (H, W) for 2D images).
   :type dim: int or tuple of int
   :param strategy: Embedding strategy name (e.g., "absolute", "pos1d", "rope1d",
                    "pos2d", "rope2d").
   :type strategy: str
   :param semantic_id: Semantic identifier for the token group (0=obs, 1=cond).
   :type semantic_id: int
   :param size: Patch edge length for 2D strategies (default 2). Ignored for 1D
                strategies. Use 1 for no patchification.
   :type size: int, optional

   :returns: * **ids** (*Array*) -- Token ID array.
             * **resolved_dim** (*int*) -- Resolved flat dimension.

   :raises ValueError: If ``strategy`` is not recognized.















   ..
       !! processed by numpydoc !!

.. py:function:: _single_obs(x_o, *, channel, name='x_o')

   
   Canonicalize a single conditioning observation, then enforce batch == 1.

   Shape handling comes FIRST so a misshaped input can never be misread as a
   batch (e.g. ``(dim, C)`` read as ``dim`` observations):

   - ``channel="require"``: tabular flow-pipeline contract — input must
     already carry batch and channel axes ``(1, dim, C)``; channel-less input
     raises ``ValueError`` (same :func:`_require_channel` as training).
   - ``channel="promote"``: FM-pipeline contract — lenient promotion:
     ``(dim,) -> (1, dim, 1)`` and ``(B, dim) -> (B, dim, 1)``.
   - ``channel="none"``: structured inputs — the model owns the trailing
     shape; only a leading batch axis (``ndim >= 2``) is required.

   A leading batch axis > 1 then raises ``ValueError``: single-observation
   methods never silently discard observations — use ``sample_batched``.
   Returns the canonicalized array with its size-1 batch axis kept.















   ..
       !! processed by numpydoc !!

.. py:function:: build_edm_path(sde, config)

   
   Build an EDM-family diffusion path from an SDE type string and config.

   :param sde: SDE type: ``"EDM"``, ``"VE"``, or ``"VP"``.
   :type sde: str
   :param config: Training configuration dict; scheduler hyperparameters are read from
                  here with sensible defaults.
   :type config: dict

   :returns: Configured diffusion path.
   :rtype: EDMPath

   :raises ValueError: If ``sde`` is not one of ``{"EDM", "VE", "VP"}``.















   ..
       !! processed by numpydoc !!

.. py:function:: build_sm_path(sde_type, config)

   
   Build a score-matching path from an SDE type string and config.

   :param sde_type: SDE type: ``"VP"`` or ``"VE"``.
   :type sde_type: str
   :param config: Training configuration dict; scheduler hyperparameters are read from
                  here with sensible defaults.
   :type config: dict

   :returns: Configured score-matching path.
   :rtype: SMPath

   :raises ValueError: If ``sde_type`` is not one of ``{"VP", "VE"}``.















   ..
       !! processed by numpydoc !!

.. py:function:: init_ids_1d(dim, semantic_id = None)

   
   Build 1D positional IDs, returning ``(ids, dim)``.

   ``ids`` is ``(1, dim, 1)`` when ``semantic_id is None`` (position only), or
   ``(1, dim, 2)`` otherwise, with the position at axis 0 and the semantic id
   at axis 1.

   FIXME (axis-order footgun): this places the semantic id on the LAST axis,
   which is the *reverse* of :func:`init_ids_2d` (semantic at axis 0, then h, w
   -- the established convention). The two are safe in isolation, but they do
   NOT line up if 1D and 2D ids are ever fed into the same RoPE grid (e.g.
   FieldDiT Phase-2 obs+cond co-tokenization with a shared ``EmbedND``, where
   ``axes_dim[i]`` is matched to ids axis ``i``). This should be unified to the
   2D convention (semantic at axis 0). It is not changed here because callers
   that pass ``semantic_id`` -- notably the Flux1 ``rope1d`` path via
   :func:`init_ids` -- and any code indexing ``ids[..., k]`` must be updated in
   lockstep.















   ..
       !! processed by numpydoc !!

.. py:function:: init_ids_2d(dim, semantic_id = 0, size = 2)

   
   Build 2D positional IDs for a patchified image grid.

   The grid has one entry per patch, i.e. ``(dim[0] // size, dim[1] // size)``,
   matching ``patchify_2d(x, size=size)``. ``size`` is the patch edge length;
   use ``size=1`` for no patchification (one token per pixel).















   ..
       !! processed by numpydoc !!

.. py:function:: init_ids_joint(dim_obs, dim_cond)

.. py:function:: parse_training_config(config_path)

   
   Parse training and optimizer configuration from a YAML config file.

   Reads the ``training`` and ``optimizer`` sections of the config and
   returns a flat dictionary consumed by :class:`AbstractPipeline`.

   :param config_path: Path to the YAML configuration file.
   :type config_path: str

   :returns: **training_config** -- Parsed training configuration dictionary.
   :rtype: dict















   ..
       !! processed by numpydoc !!

.. py:function:: scale_lr(batch_size, base_lr=0.0001, reference_batch_size=256)

   
   Scale learning rate based on batch size using square root scaling.

   :param batch_size: The current batch size.
   :type batch_size: int
   :param base_lr: The base learning rate for the reference batch size.
   :type base_lr: float
   :param reference_batch_size: The reference batch size. Defaults to 256.
   :type reference_batch_size: int, optional

   :returns: The adjusted learning rate.
   :rtype: float















   ..
       !! processed by numpydoc !!

.. py:data:: _EMBEDDINGS_1D

.. py:data:: _EMBEDDINGS_2D

.. py:data:: _MOVED_TO_PATCHING
   :value: ('patchify_2d', 'depatchify_2d')


