gensbi.utils.model_wrapping
===========================

.. py:module:: gensbi.utils.model_wrapping

.. autoapi-nested-parse::

   Model wrapping utilities for GenSBI.

   This module provides wrapper classes for models used in flow matching and diffusion,
   facilitating integration with ODE solvers and providing utilities for computing
   vector fields and divergences.

   ..
       !! processed by numpydoc !!


Classes
-------

.. autoapisummary::

   gensbi.utils.model_wrapping.ModelWrapper
   gensbi.utils.model_wrapping.ScoreToDrift


Module Contents
---------------

.. py:class:: ModelWrapper(model)

   Bases: :py:obj:`flax.nnx.Module`


   
   Wrapper class for models to provide ODE solver integration.

   This class wraps around another model and provides methods for computing
   the vector field and divergence, which are useful for ODE solvers that
   require these quantities.

   :param model:
   :type model: The model to wrap.















   ..
       !! processed by numpydoc !!

   .. py:method:: __call__(t, obs, **kwargs)

      
      Call the wrapped model with ``obs`` and ``t``.

      Uses keyword arguments when calling the underlying model for
      safety (avoids positional-argument order bugs).

      :param t: time (batch_size).
      :type t: Array
      :param obs: input data to the model (batch_size, ...).
      :type obs: Array
      :param \*\*kwargs: e.g., text condition.
      :type \*\*kwargs: additional information forwarded to the model,

      :returns: model output.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:method:: get_divergence(exact = True, **kwargs)

      
      Return a function that computes the divergence of the vector field.

      :param exact: If ``True`` (default), compute the exact divergence via
                    the full Jacobian (``jax.jacfwd`` + trace).  If ``False``,
                    use the Hutchinson stochastic trace estimator (single JVP
                    with a Rademacher probe).  The Hutchinson variant requires
                    the probe vector to be passed at call time inside
                    ``args["div_v"]``.
      :type exact: bool
      :param \*\*kwargs: Static keyword arguments forwarded to ``get_vector_field``.

      :returns: ``div_(t, x, args)`` — divergence function compatible with
                diffrax ODE terms.
      :rtype: Callable















      ..
          !! processed by numpydoc !!


   .. py:method:: get_vector_field(**kwargs)

      
      Compute the vector field of the model, properly squeezed for the ODE term.

      :param x: input data to the model (batch_size, ...).
      :type x: Array
      :param t: time (batch_size).
      :type t: Array
      :param args:
      :type args: additional information forwarded to the model, e.g., text condition.

      :returns: vector field of the model.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:attribute:: model


.. py:class:: ScoreToDrift(score_model, sde)

   Bases: :py:obj:`flax.nnx.Module`


   
   Thin adapter that makes a score model look like a velocity (drift) model.

   When called as ``model(obs, t, **kwargs)``, returns the PF-ODE drift
   instead of the raw score:

   .. math::
       u(x, t) = f(x, t) - \tfrac{1}{2}\, g(t)^2\, s_\theta(x, t)

   This allows passing the adapted model to **existing** wrappers
   (``ModelWrapper``, ``JointWrapper``, ``ConditionalWrapper``, etc.)
   without needing SM-specific wrapper subclasses.

   :param score_model: The score model, called as ``score_model(obs, t, **kwargs)``.
   :param sde: The SDE scheduler (e.g. ``VPSmScheduler`` or ``VESmScheduler``).

   .. rubric:: Example

   .. code-block:: python

       drift_model = ScoreToDrift(score_model, sde)
       wrapper = ModelWrapper(drift_model)    # or JointWrapper(drift_model)
       solver = ODESolver(velocity_model=wrapper)















   ..
       !! processed by numpydoc !!

   .. py:method:: __call__(obs, t, **kwargs)

      
      Return PF-ODE drift for inputs ``(obs, t)``.
















      ..
          !! processed by numpydoc !!


   .. py:attribute:: score_model


   .. py:attribute:: sde


