gensbi.diffusion.solver
=======================

.. py:module:: gensbi.diffusion.solver

.. autoapi-nested-parse::

   
   Solvers for generative diffusion models.

   This module provides SDE solvers specifically designed for sampling from generative
   diffusion models, including stochastic differential equation integration methods
   as detailed in the EDM paper "Elucidating the Design Space of Diffusion-Based
   Generative Models" (Karras et al., 2022) and standard score matching samplers
   from "Score-Based Generative Modeling through Stochastic Differential Equations"
   (Song et al., 2021).















   ..
       !! processed by numpydoc !!


Submodules
----------

.. toctree::
   :maxdepth: 1

   /api/gensbi/diffusion/solver/edm_samplers/index
   /api/gensbi/diffusion/solver/edm_solver/index
   /api/gensbi/diffusion/solver/sm_ode_solver/index
   /api/gensbi/diffusion/solver/sm_sde_solver/index


Classes
-------

.. autoapisummary::

   gensbi.diffusion.solver.EDMSolver
   gensbi.diffusion.solver.SMODESolver
   gensbi.diffusion.solver.SMSDESolver


Package Contents
----------------

.. py:class:: EDMSolver(score_model, path)

   Bases: :py:obj:`gensbi.solver.Solver`


   
   Abstract base class for generative model solvers.
















   ..
       !! processed by numpydoc !!

   .. py:method:: get_sampler(condition_mask = None, condition_value = None, cfg_scale = None, nsteps = 18, method = 'Heun', return_intermediates = False, static_model_kwargs = None, solver_params = None, solver_scheduler = None)

      
      Returns a sampler function for the SDE.

      :param condition_mask: Mask for conditioning.
      :type condition_mask: Optional[Array]
      :param condition_value: Value for conditioning.
      :type condition_value: Optional[Array]
      :param cfg_scale: Classifier-free guidance scale (not implemented).
      :type cfg_scale: Optional[float]
      :param nsteps: Number of steps.
      :type nsteps: int
      :param method: Integration method.
      :type method: str
      :param return_intermediates: Whether to return intermediate steps.
      :type return_intermediates: bool
      :param static_model_kwargs: Static model arguments baked into the sampler.
                                  Condition-dependent data should be passed at call time
                                  via ``model_extras``.
      :type static_model_kwargs: dict
      :param solver_params: Additional solver parameters.
      :type solver_params: Optional[dict]
      :param solver_scheduler: Scheduler to use for the solver. If None, the path's scheduler is used.
      :type solver_scheduler: Optional[Any]

      :returns: ``sample(key, x_init, model_extras=None)`` sampler function.
      :rtype: Callable















      ..
          !! processed by numpydoc !!


   .. py:method:: sample(key, x_init, condition_mask = None, condition_value = None, cfg_scale = None, nsteps = 18, method = 'Heun', return_intermediates = False, model_extras = None, solver_params = None, solver_scheduler = None)

      
      Sample from the SDE using the sampler.

      :param key: JAX random key.
      :type key: Array
      :param x_init: Initial value.
      :type x_init: Array
      :param condition_mask: Mask for conditioning.
      :type condition_mask: Optional[Array]
      :param condition_value: Value for conditioning.
      :type condition_value: Optional[Array]
      :param cfg_scale: Classifier-free guidance scale (not implemented).
      :type cfg_scale: Optional[float]
      :param nsteps: Number of steps.
      :type nsteps: int
      :param method: Integration method.
      :type method: str
      :param return_intermediates: Whether to return intermediate steps.
      :type return_intermediates: bool
      :param model_extras: Runtime model extras (e.g. ``cond``, ``obs_ids``).
      :type model_extras: dict
      :param solver_params: Additional solver parameters.
      :type solver_params: Optional[dict]
      :param solver_scheduler: Scheduler to use for the solver. If None, the path's scheduler is used.
      :type solver_scheduler: Optional[Any]

      :returns: Sampled output.
      :rtype: Array















      ..
          !! processed by numpydoc !!


   .. py:attribute:: path


   .. py:attribute:: score_model


.. py:class:: SMODESolver(velocity_model)

   Bases: :py:obj:`gensbi.core.ode_solver.ODESolver`


   
   Score matching probability flow ODE solver.

   Uses the probability flow ODE formulation to sample deterministically
   from a score matching model.  The velocity model passed to this solver
   should already be wrapped with ``ScoreToODEDrift`` + ``ModelWrapper``
   by the pipeline.

   All integration and log-probability logic is inherited from
   :class:`~gensbi.core.ode_solver.ODESolver`.

   .. seealso:: :py:obj:`gensbi.utils.model_wrapping.ScoreToODEDrift`, :py:obj:`gensbi.core.score_matching.ScoreMatchingMethod.build_solver`















   ..
       !! processed by numpydoc !!

   .. py:method:: get_drift(**kwargs)

      
      Return the probability flow ODE drift (from ScoreToODEDrift adapter).
















      ..
          !! processed by numpydoc !!


.. py:class:: SMSDESolver(velocity_model, sde, eps0 = 0.001)

   Bases: :py:obj:`gensbi.core.sde_solver.SDESolver`


   
   Score matching reverse SDE solver.

   The drift and diffusion are computed inline from the raw score model
   and the forward SDE scheduler, analogous to how ``ZeroEndsSolver``
   computes its SDE coefficients from the velocity field.

   Conditioning is handled entirely by the ``ModelWrapper`` layer
   (``ConditionalWrapper``, ``JointWrapper``).

   :param velocity_model: Wrapped **score** model.  ``get_vector_field()`` returns the
                          raw score function.
   :type velocity_model: ModelWrapper
   :param sde: Forward SDE scheduler (``VPSmScheduler``, ``VESmScheduler``, etc.)
               providing ``drift(x, t)`` and ``diffusion(t)``.
   :param eps0: Minimum time value.
   :type eps0: float















   ..
       !! processed by numpydoc !!

   .. py:method:: get_diffusion()

      
      Return the reverse SDE diffusion.

      .. math::
          \tilde{g}(t) = g(t)

      Returns a ``(flat_dim, flat_dim)`` diagonal matrix.















      ..
          !! processed by numpydoc !!


   .. py:method:: get_drift(**kwargs)

      
      Return the reverse SDE drift.

      .. math::
          \tilde{f}(t, x) = f(x, t) - g(t)^2\, s_\theta(x, t)

      where :math:`f` and :math:`g` are the forward SDE coefficients
      and :math:`s_\theta` is the score model.















      ..
          !! processed by numpydoc !!


   .. py:attribute:: sde


