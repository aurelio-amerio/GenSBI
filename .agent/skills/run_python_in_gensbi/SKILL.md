---
name: Run Python in Gensbi Environment
description: Instructions for running python inside the gensbi uv environment.
---

# Running Python in GenSBI Environment

When you need to run any python command, script, or module in the terminal within this workspace, use `uv run`:

```bash
uv run python script.py arg1 arg2
uv run python -m my.module
```
